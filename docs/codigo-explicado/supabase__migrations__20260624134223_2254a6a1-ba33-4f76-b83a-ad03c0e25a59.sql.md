# supabase/migrations/20260624134223_2254a6a1-ba33-4f76-b83a-ad03c0e25a59.sql

Tipo: Migration SQL do Supabase.

Este documento foi gerado para estudo e manutencao. Ele preserva o codigo original e explica linha por linha sem alterar o comportamento do sistema publicado.

| Linha | Codigo original | Explicacao |
| ---: | --- | --- |
| 1 | `(linha em branco)` | Separa blocos de codigo para melhorar a leitura. |
| 2 | `DROP POLICY IF EXISTS task_attachments_select_auth ON storage.objects;` | Remove objeto antigo para permitir recriacao ou limpeza controlada. |
| 3 | `DROP POLICY IF EXISTS task_attachments_delete_own ON storage.objects;` | Remove objeto antigo para permitir recriacao ou limpeza controlada. |
| 4 | `(linha em branco)` | Separa blocos de codigo para melhorar a leitura. |
| 5 | `CREATE POLICY task_attachments_select_auth` | Cria uma regra de seguranca RLS para controlar acesso aos dados. |
| 6 | `ON storage.objects FOR SELECT` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 7 | `TO authenticated` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 8 | `USING (bucket_id = 'task-attachments');` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 9 | `(linha em branco)` | Separa blocos de codigo para melhorar a leitura. |
| 10 | `CREATE POLICY task_attachments_delete_own` | Cria uma regra de seguranca RLS para controlar acesso aos dados. |
| 11 | `ON storage.objects FOR DELETE` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 12 | `TO authenticated` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 13 | `USING (` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 14 | `  bucket_id = 'task-attachments'` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 15 | `  AND (` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 16 | `    owner = auth.uid()` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 17 | `    OR has_role(auth.uid(), 'admin'::app_role)` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 18 | `    OR EXISTS (SELECT 1 FROM public.attachments a WHERE a.storage_path = objects.name AND a.uploaded_by = auth.uid())` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 19 | `    OR EXISTS (SELECT 1 FROM public.comment_attachments ca WHERE ca.storage_path = objects.name AND ca.uploaded_by = auth.uid())` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 20 | `    OR EXISTS (SELECT 1 FROM public.client_note_attachments na WHERE na.storage_path = objects.name AND na.uploaded_by = auth.uid())` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 21 | `    OR EXISTS (SELECT 1 FROM public.client_files cf WHERE cf.storage_path = objects.name AND cf.uploaded_by = auth.uid())` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 22 | `  )` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 23 | `);` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 24 | `(linha em branco)` | Separa blocos de codigo para melhorar a leitura. |
