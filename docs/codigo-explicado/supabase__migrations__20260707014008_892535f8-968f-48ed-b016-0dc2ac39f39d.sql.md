# supabase/migrations/20260707014008_892535f8-968f-48ed-b016-0dc2ac39f39d.sql

Tipo: Migration SQL do Supabase.

Este documento foi gerado para estudo e manutencao. Ele preserva o codigo original e explica linha por linha sem alterar o comportamento do sistema publicado.

| Linha | Codigo original | Explicacao |
| ---: | --- | --- |
| 1 | `(linha em branco)` | Separa blocos de codigo para melhorar a leitura. |
| 2 | `-- Allow tasks to have no status and no priority` | Comentario da migration; explica a intencao daquele trecho SQL. |
| 3 | `ALTER TABLE public.tasks ALTER COLUMN status DROP NOT NULL;` | Altera uma tabela existente, normalmente adicionando campos, chaves ou politicas. |
| 4 | `ALTER TABLE public.tasks ALTER COLUMN priority DROP NOT NULL;` | Altera uma tabela existente, normalmente adicionando campos, chaves ou politicas. |
| 5 | `(linha em branco)` | Separa blocos de codigo para melhorar a leitura. |
| 6 | `-- Add due_date to subtasks` | Comentario da migration; explica a intencao daquele trecho SQL. |
| 7 | `ALTER TABLE public.subtasks ADD COLUMN IF NOT EXISTS due_date TIMESTAMPTZ;` | Altera uma tabela existente, normalmente adicionando campos, chaves ou politicas. |
| 8 | `(linha em branco)` | Separa blocos de codigo para melhorar a leitura. |
| 9 | `-- Subtask due-date change log` | Comentario da migration; explica a intencao daquele trecho SQL. |
| 10 | `CREATE TABLE IF NOT EXISTS public.subtask_due_date_changes (` | Cria uma tabela no banco de dados Supabase/PostgreSQL. |
| 11 | `  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),` | Define identificador unico principal do registro. |
| 12 | `  subtask_id UUID NOT NULL REFERENCES public.subtasks(id) ON DELETE CASCADE,` | Define relacionamento entre tabelas por chave estrangeira. |
| 13 | `  old_due_date TIMESTAMPTZ,` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 14 | `  new_due_date TIMESTAMPTZ,` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 15 | `  reason TEXT,` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 16 | `  user_id UUID,` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 17 | `  created_at TIMESTAMPTZ NOT NULL DEFAULT now()` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 18 | `);` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 19 | `(linha em branco)` | Separa blocos de codigo para melhorar a leitura. |
| 20 | `GRANT SELECT, INSERT, UPDATE, DELETE ON public.subtask_due_date_changes TO authenticated;` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 21 | `GRANT ALL ON public.subtask_due_date_changes TO service_role;` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 22 | `(linha em branco)` | Separa blocos de codigo para melhorar a leitura. |
| 23 | `ALTER TABLE public.subtask_due_date_changes ENABLE ROW LEVEL SECURITY;` | Altera uma tabela existente, normalmente adicionando campos, chaves ou politicas. |
| 24 | `(linha em branco)` | Separa blocos de codigo para melhorar a leitura. |
| 25 | `CREATE POLICY "View subtask due changes for viewable tasks"` | Cria uma regra de seguranca RLS para controlar acesso aos dados. |
| 26 | `  ON public.subtask_due_date_changes FOR SELECT TO authenticated` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 27 | `  USING (` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 28 | `    EXISTS (` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 29 | `      SELECT 1 FROM public.subtasks s` | Consulta dados ou valida uma condicao no banco. |
| 30 | `      WHERE s.id = subtask_due_date_changes.subtask_id` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 31 | `        AND public.can_view_task(s.task_id)` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 32 | `    )` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 33 | `  );` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 34 | `(linha em branco)` | Separa blocos de codigo para melhorar a leitura. |
| 35 | `CREATE POLICY "Insert subtask due changes for viewable tasks"` | Cria uma regra de seguranca RLS para controlar acesso aos dados. |
| 36 | `  ON public.subtask_due_date_changes FOR INSERT TO authenticated` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 37 | `  WITH CHECK (` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 38 | `    EXISTS (` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 39 | `      SELECT 1 FROM public.subtasks s` | Consulta dados ou valida uma condicao no banco. |
| 40 | `      WHERE s.id = subtask_due_date_changes.subtask_id` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 41 | `        AND public.can_view_task(s.task_id)` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 42 | `    )` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 43 | `  );` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 44 | `(linha em branco)` | Separa blocos de codigo para melhorar a leitura. |
| 45 | `CREATE POLICY "Delete own subtask due changes"` | Cria uma regra de seguranca RLS para controlar acesso aos dados. |
| 46 | `  ON public.subtask_due_date_changes FOR DELETE TO authenticated` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 47 | `  USING (user_id = auth.uid() OR public.has_role(auth.uid(), 'admin'::app_role));` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 48 | `(linha em branco)` | Separa blocos de codigo para melhorar a leitura. |
| 49 | `CREATE INDEX IF NOT EXISTS idx_subtask_due_date_changes_subtask ON public.subtask_due_date_changes(subtask_id, created_at DESC);` | Cria indice para acelerar consultas frequentes. |
| 50 | `(linha em branco)` | Separa blocos de codigo para melhorar a leitura. |
