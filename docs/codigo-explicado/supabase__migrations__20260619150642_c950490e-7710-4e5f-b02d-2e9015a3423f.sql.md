# supabase/migrations/20260619150642_c950490e-7710-4e5f-b02d-2e9015a3423f.sql

Tipo: Migration SQL do Supabase.

Este documento foi gerado para estudo e manutencao. Ele preserva o codigo original e explica linha por linha sem alterar o comportamento do sistema publicado.

| Linha | Codigo original | Explicacao |
| ---: | --- | --- |
| 1 | `(linha em branco)` | Separa blocos de codigo para melhorar a leitura. |
| 2 | `-- 1) Per-card width (nullable; null = use board default)` | Comentario da migration; explica a intencao daquele trecho SQL. |
| 3 | `ALTER TABLE public.tasks` | Altera uma tabela existente, normalmente adicionando campos, chaves ou politicas. |
| 4 | `  ADD COLUMN IF NOT EXISTS card_width integer` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 5 | `    CHECK (card_width IS NULL OR (card_width BETWEEN 240 AND 800));` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 6 | `(linha em branco)` | Separa blocos de codigo para melhorar a leitura. |
| 7 | `-- 2) Attachments per observation (comment)` | Comentario da migration; explica a intencao daquele trecho SQL. |
| 8 | `CREATE TABLE IF NOT EXISTS public.comment_attachments (` | Cria uma tabela no banco de dados Supabase/PostgreSQL. |
| 9 | `  id uuid PRIMARY KEY DEFAULT gen_random_uuid(),` | Define identificador unico principal do registro. |
| 10 | `  comment_id uuid NOT NULL REFERENCES public.comments(id) ON DELETE CASCADE,` | Define relacionamento entre tabelas por chave estrangeira. |
| 11 | `  task_id uuid NOT NULL REFERENCES public.tasks(id) ON DELETE CASCADE,` | Define relacionamento entre tabelas por chave estrangeira. |
| 12 | `  file_name text NOT NULL,` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 13 | `  storage_path text NOT NULL UNIQUE,` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 14 | `  mime_type text,` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 15 | `  size_bytes bigint,` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 16 | `  uploaded_by uuid REFERENCES auth.users(id) ON DELETE SET NULL,` | Define relacionamento entre tabelas por chave estrangeira. |
| 17 | `  created_at timestamptz NOT NULL DEFAULT now()` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 18 | `);` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 19 | `(linha em branco)` | Separa blocos de codigo para melhorar a leitura. |
| 20 | `CREATE INDEX IF NOT EXISTS idx_comment_attachments_comment ON public.comment_attachments(comment_id);` | Cria indice para acelerar consultas frequentes. |
| 21 | `CREATE INDEX IF NOT EXISTS idx_comment_attachments_task ON public.comment_attachments(task_id);` | Cria indice para acelerar consultas frequentes. |
| 22 | `(linha em branco)` | Separa blocos de codigo para melhorar a leitura. |
| 23 | `GRANT SELECT, INSERT, UPDATE, DELETE ON public.comment_attachments TO authenticated;` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 24 | `GRANT ALL ON public.comment_attachments TO service_role;` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 25 | `(linha em branco)` | Separa blocos de codigo para melhorar a leitura. |
| 26 | `ALTER TABLE public.comment_attachments ENABLE ROW LEVEL SECURITY;` | Altera uma tabela existente, normalmente adicionando campos, chaves ou politicas. |
| 27 | `(linha em branco)` | Separa blocos de codigo para melhorar a leitura. |
| 28 | `CREATE POLICY "comment_attachments_select" ON public.comment_attachments` | Cria uma regra de seguranca RLS para controlar acesso aos dados. |
| 29 | `  FOR SELECT TO authenticated` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 30 | `  USING (public.can_view_task(task_id));` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 31 | `(linha em branco)` | Separa blocos de codigo para melhorar a leitura. |
| 32 | `CREATE POLICY "comment_attachments_insert" ON public.comment_attachments` | Cria uma regra de seguranca RLS para controlar acesso aos dados. |
| 33 | `  FOR INSERT TO authenticated` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 34 | `  WITH CHECK (uploaded_by = auth.uid() AND public.can_view_task(task_id));` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 35 | `(linha em branco)` | Separa blocos de codigo para melhorar a leitura. |
| 36 | `CREATE POLICY "comment_attachments_delete" ON public.comment_attachments` | Cria uma regra de seguranca RLS para controlar acesso aos dados. |
| 37 | `  FOR DELETE TO authenticated` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 38 | `  USING (public.can_view_task(task_id));` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 39 | `(linha em branco)` | Separa blocos de codigo para melhorar a leitura. |
| 40 | `-- 3) Extend storage SELECT/DELETE policies to also accept comment_attachments` | Comentario da migration; explica a intencao daquele trecho SQL. |
| 41 | `DROP POLICY IF EXISTS task_attachments_select_auth ON storage.objects;` | Remove objeto antigo para permitir recriacao ou limpeza controlada. |
| 42 | `CREATE POLICY task_attachments_select_auth ON storage.objects` | Cria uma regra de seguranca RLS para controlar acesso aos dados. |
| 43 | `  FOR SELECT TO authenticated` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 44 | `  USING (` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 45 | `    bucket_id = 'task-attachments'` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 46 | `    AND (` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 47 | `      EXISTS (` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 48 | `        SELECT 1 FROM public.attachments a` | Consulta dados ou valida uma condicao no banco. |
| 49 | `        JOIN public.tasks t ON t.id = a.task_id` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 50 | `        WHERE a.storage_path = objects.name` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 51 | `          AND (t.created_by = auth.uid() OR t.assignee_id = auth.uid() OR public.has_role(auth.uid(), 'admin'::app_role))` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 52 | `      )` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 53 | `      OR EXISTS (` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 54 | `        SELECT 1 FROM public.comment_attachments ca` | Consulta dados ou valida uma condicao no banco. |
| 55 | `        JOIN public.tasks t ON t.id = ca.task_id` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 56 | `        WHERE ca.storage_path = objects.name` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 57 | `          AND (t.created_by = auth.uid() OR t.assignee_id = auth.uid() OR public.has_role(auth.uid(), 'admin'::app_role))` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 58 | `      )` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 59 | `    )` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 60 | `  );` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 61 | `(linha em branco)` | Separa blocos de codigo para melhorar a leitura. |
| 62 | `DROP POLICY IF EXISTS task_attachments_delete_own ON storage.objects;` | Remove objeto antigo para permitir recriacao ou limpeza controlada. |
| 63 | `CREATE POLICY task_attachments_delete_own ON storage.objects` | Cria uma regra de seguranca RLS para controlar acesso aos dados. |
| 64 | `  FOR DELETE TO authenticated` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 65 | `  USING (` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 66 | `    bucket_id = 'task-attachments'` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 67 | `    AND (` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 68 | `      EXISTS (SELECT 1 FROM public.attachments a WHERE a.storage_path = objects.name AND a.uploaded_by = auth.uid())` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 69 | `      OR EXISTS (SELECT 1 FROM public.comment_attachments ca WHERE ca.storage_path = objects.name AND ca.uploaded_by = auth.uid())` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 70 | `    )` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 71 | `  );` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 72 | `(linha em branco)` | Separa blocos de codigo para melhorar a leitura. |
