# supabase/migrations/20260622223004_1c13e5d9-9944-431f-9959-bde69fa3147e.sql

Tipo: Migration SQL do Supabase.

Este documento foi gerado para estudo e manutencao. Ele preserva o codigo original e explica linha por linha sem alterar o comportamento do sistema publicado.

| Linha | Codigo original | Explicacao |
| ---: | --- | --- |
| 1 | `(linha em branco)` | Separa blocos de codigo para melhorar a leitura. |
| 2 | `-- 1. comment_attachments: add UPDATE policy restricted to uploader/admin` | Comentario da migration; explica a intencao daquele trecho SQL. |
| 3 | `CREATE POLICY "comment_attachments_update"` | Cria uma regra de seguranca RLS para controlar acesso aos dados. |
| 4 | `ON public.comment_attachments` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 5 | `FOR UPDATE` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 6 | `TO authenticated` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 7 | `USING (uploaded_by = auth.uid() OR public.has_role(auth.uid(), 'admin'))` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 8 | `WITH CHECK (uploaded_by = auth.uid() OR public.has_role(auth.uid(), 'admin'));` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 9 | `(linha em branco)` | Separa blocos de codigo para melhorar a leitura. |
| 10 | `-- 2. profiles: restrict email column at the privilege level` | Comentario da migration; explica a intencao daquele trecho SQL. |
| 11 | `REVOKE SELECT ON public.profiles FROM authenticated, anon;` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 12 | `GRANT SELECT (id, full_name, avatar_url, color, theme_preferences, is_active, created_at, updated_at)` | Define relacionamento entre tabelas por chave estrangeira. |
| 13 | `  ON public.profiles TO authenticated;` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 14 | `(linha em branco)` | Separa blocos de codigo para melhorar a leitura. |
| 15 | `-- Admin-only RPC to fetch emails (used by the users admin page)` | Comentario da migration; explica a intencao daquele trecho SQL. |
| 16 | `CREATE OR REPLACE FUNCTION public.admin_get_profile_emails()` | Define uma funcao no banco para reutilizar logica SQL. |
| 17 | `RETURNS TABLE(id uuid, email text)` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 18 | `LANGUAGE sql` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 19 | `STABLE` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 20 | `SECURITY DEFINER` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 21 | `SET search_path = public` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 22 | `AS $$` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 23 | `  SELECT p.id, p.email` | Consulta dados ou valida uma condicao no banco. |
| 24 | `  FROM public.profiles p` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 25 | `  WHERE public.has_role(auth.uid(), 'admin');` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 26 | `$$;` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 27 | `REVOKE ALL ON FUNCTION public.admin_get_profile_emails() FROM PUBLIC;` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 28 | `GRANT EXECUTE ON FUNCTION public.admin_get_profile_emails() TO authenticated;` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 29 | `(linha em branco)` | Separa blocos de codigo para melhorar a leitura. |
| 30 | `-- 3. task_history: ensure inserts are only for viewable tasks` | Comentario da migration; explica a intencao daquele trecho SQL. |
| 31 | `DROP POLICY IF EXISTS hist_insert_self ON public.task_history;` | Remove objeto antigo para permitir recriacao ou limpeza controlada. |
| 32 | `CREATE POLICY hist_insert_self` | Cria uma regra de seguranca RLS para controlar acesso aos dados. |
| 33 | `ON public.task_history` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 34 | `FOR INSERT` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 35 | `TO authenticated` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 36 | `WITH CHECK (auth.uid() = user_id AND public.can_view_task(task_id));` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 37 | `(linha em branco)` | Separa blocos de codigo para melhorar a leitura. |
| 38 | `-- 4. user_roles: only own row visible to self; admins still see all (via roles_admin_manage)` | Comentario da migration; explica a intencao daquele trecho SQL. |
| 39 | `DROP POLICY IF EXISTS roles_select_all_auth ON public.user_roles;` | Remove objeto antigo para permitir recriacao ou limpeza controlada. |
| 40 | `CREATE POLICY roles_select_own` | Cria uma regra de seguranca RLS para controlar acesso aos dados. |
| 41 | `ON public.user_roles` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 42 | `FOR SELECT` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 43 | `TO authenticated` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 44 | `USING (user_id = auth.uid() OR public.has_role(auth.uid(), 'admin'));` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 45 | `(linha em branco)` | Separa blocos de codigo para melhorar a leitura. |
| 46 | `-- 5. storage.objects: explicit restrictive UPDATE policy for task-attachments` | Comentario da migration; explica a intencao daquele trecho SQL. |
| 47 | `CREATE POLICY "task_attachments_update_own"` | Cria uma regra de seguranca RLS para controlar acesso aos dados. |
| 48 | `ON storage.objects` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 49 | `FOR UPDATE` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 50 | `TO authenticated` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 51 | `USING (bucket_id = 'task-attachments' AND owner = auth.uid())` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 52 | `WITH CHECK (bucket_id = 'task-attachments' AND owner = auth.uid());` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 53 | `(linha em branco)` | Separa blocos de codigo para melhorar a leitura. |
