# supabase/migrations/20260720201842_94ca78dd-75ad-4995-9cce-2d91f79e0847.sql

Tipo: Migration SQL do Supabase.

Este documento foi gerado para estudo e manutencao. Ele preserva o codigo original e explica linha por linha sem alterar o comportamento do sistema publicado.

| Linha | Codigo original | Explicacao |
| ---: | --- | --- |
| 1 | `(linha em branco)` | Separa blocos de codigo para melhorar a leitura. |
| 2 | `ALTER TABLE public.subtasks` | Altera uma tabela existente, normalmente adicionando campos, chaves ou politicas. |
| 3 | `  ADD COLUMN IF NOT EXISTS assignee_id uuid REFERENCES auth.users(id) ON DELETE SET NULL,` | Define relacionamento entre tabelas por chave estrangeira. |
| 4 | `  ADD COLUMN IF NOT EXISTS notes text;` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 5 | `(linha em branco)` | Separa blocos de codigo para melhorar a leitura. |
| 6 | `-- Move subtarefas que estavam presas a comentários para o nível raiz da tarefa` | Comentario da migration; explica a intencao daquele trecho SQL. |
| 7 | `UPDATE public.subtasks SET comment_id = NULL WHERE comment_id IS NOT NULL;` | Atualiza registros existentes no banco. |
| 8 | `(linha em branco)` | Separa blocos de codigo para melhorar a leitura. |
| 9 | `CREATE TABLE IF NOT EXISTS public.subtask_attachments (` | Cria uma tabela no banco de dados Supabase/PostgreSQL. |
| 10 | `  id uuid PRIMARY KEY DEFAULT gen_random_uuid(),` | Define identificador unico principal do registro. |
| 11 | `  subtask_id uuid NOT NULL REFERENCES public.subtasks(id) ON DELETE CASCADE,` | Define relacionamento entre tabelas por chave estrangeira. |
| 12 | `  task_id uuid NOT NULL REFERENCES public.tasks(id) ON DELETE CASCADE,` | Define relacionamento entre tabelas por chave estrangeira. |
| 13 | `  file_name text NOT NULL,` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 14 | `  storage_path text NOT NULL,` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 15 | `  mime_type text,` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 16 | `  size_bytes bigint,` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 17 | `  uploaded_by uuid REFERENCES auth.users(id) ON DELETE SET NULL,` | Define relacionamento entre tabelas por chave estrangeira. |
| 18 | `  created_at timestamptz NOT NULL DEFAULT now()` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 19 | `);` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 20 | `GRANT SELECT, INSERT, UPDATE, DELETE ON public.subtask_attachments TO authenticated;` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 21 | `GRANT ALL ON public.subtask_attachments TO service_role;` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 22 | `ALTER TABLE public.subtask_attachments ENABLE ROW LEVEL SECURITY;` | Altera uma tabela existente, normalmente adicionando campos, chaves ou politicas. |
| 23 | `DROP POLICY IF EXISTS "subtask_attachments_all" ON public.subtask_attachments;` | Remove objeto antigo para permitir recriacao ou limpeza controlada. |
| 24 | `CREATE POLICY "subtask_attachments_all" ON public.subtask_attachments` | Cria uma regra de seguranca RLS para controlar acesso aos dados. |
| 25 | `  FOR ALL USING (public.can_view_task(task_id)) WITH CHECK (public.can_view_task(task_id));` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 26 | `(linha em branco)` | Separa blocos de codigo para melhorar a leitura. |
| 27 | `-- can_view_task passa a considerar responsáveis de subtarefas também` | Comentario da migration; explica a intencao daquele trecho SQL. |
| 28 | `CREATE OR REPLACE FUNCTION public.can_view_task(_task_id uuid)` | Define uma funcao no banco para reutilizar logica SQL. |
| 29 | `RETURNS boolean LANGUAGE sql STABLE SECURITY DEFINER SET search_path = public AS $$` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 30 | `  SELECT EXISTS (` | Consulta dados ou valida uma condicao no banco. |
| 31 | `    SELECT 1 FROM public.tasks t` | Consulta dados ou valida uma condicao no banco. |
| 32 | `    WHERE t.id = _task_id` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 33 | `      AND (` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 34 | `        public.has_role(auth.uid(), 'admin'::app_role)` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 35 | `        OR t.assignee_id = auth.uid()` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 36 | `        OR t.created_by = auth.uid()` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 37 | `        OR EXISTS (SELECT 1 FROM public.subtasks s WHERE s.task_id = t.id AND s.assignee_id = auth.uid())` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 38 | `      )` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 39 | `  )` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 40 | `$$;` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 41 | `(linha em branco)` | Separa blocos de codigo para melhorar a leitura. |
| 42 | `DROP POLICY IF EXISTS tasks_select ON public.tasks;` | Remove objeto antigo para permitir recriacao ou limpeza controlada. |
| 43 | `CREATE POLICY tasks_select ON public.tasks FOR SELECT USING (` | Cria uma regra de seguranca RLS para controlar acesso aos dados. |
| 44 | `  public.has_role(auth.uid(), 'admin'::app_role)` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 45 | `  OR assignee_id = auth.uid()` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 46 | `  OR created_by = auth.uid()` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 47 | `  OR EXISTS (SELECT 1 FROM public.subtasks s WHERE s.task_id = tasks.id AND s.assignee_id = auth.uid())` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 48 | `);` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 49 | `(linha em branco)` | Separa blocos de codigo para melhorar a leitura. |
| 50 | `-- Notificação de atribuição de subtarefa` | Comentario da migration; explica a intencao daquele trecho SQL. |
| 51 | `CREATE OR REPLACE FUNCTION public.notify_subtask_assignment()` | Define uma funcao no banco para reutilizar logica SQL. |
| 52 | `RETURNS trigger LANGUAGE plpgsql SECURITY DEFINER SET search_path = public AS $$` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 53 | `DECLARE` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 54 | `  actor uuid := auth.uid();` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 55 | `  assigner_name text;` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 56 | `  task_title text;` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 57 | `  clean_title text;` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 58 | `BEGIN` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 59 | `  IF NEW.assignee_id IS NULL THEN RETURN NEW; END IF;` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 60 | `  IF TG_OP = 'UPDATE' AND OLD.assignee_id IS NOT DISTINCT FROM NEW.assignee_id THEN RETURN NEW; END IF;` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 61 | `  IF NEW.assignee_id = actor THEN RETURN NEW; END IF;` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 62 | `(linha em branco)` | Separa blocos de codigo para melhorar a leitura. |
| 63 | `  SELECT COALESCE(full_name, email) INTO assigner_name FROM public.profiles WHERE id = actor;` | Consulta dados ou valida uma condicao no banco. |
| 64 | `  SELECT title INTO task_title FROM public.tasks WHERE id = NEW.task_id;` | Consulta dados ou valida uma condicao no banco. |
| 65 | `  clean_title := regexp_replace(COALESCE(NEW.title, ''), '<[^>]+>', '', 'g');` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 66 | `(linha em branco)` | Separa blocos de codigo para melhorar a leitura. |
| 67 | `  INSERT INTO public.notifications (user_id, task_id, type, title, body)` | Insere dados iniciais ou registros de apoio. |
| 68 | `  VALUES (` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 69 | `    NEW.assignee_id,` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 70 | `    NEW.task_id,` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 71 | `    'subtask_assignment',` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 72 | `    'Nova subtarefa atribuída a você',` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 73 | `    COALESCE(assigner_name, 'Alguém') || ' atribuiu a subtarefa "' || clean_title || '"' ||` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 74 | `    CASE WHEN task_title IS NOT NULL THEN ' em: ' || task_title ELSE '' END` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 75 | `  );` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 76 | `  RETURN NEW;` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 77 | `END;` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 78 | `$$;` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 79 | `(linha em branco)` | Separa blocos de codigo para melhorar a leitura. |
| 80 | `DROP TRIGGER IF EXISTS trg_notify_subtask_assignment ON public.subtasks;` | Remove objeto antigo para permitir recriacao ou limpeza controlada. |
| 81 | `CREATE TRIGGER trg_notify_subtask_assignment` | Cria gatilho que executa uma funcao automaticamente em eventos da tabela. |
| 82 | `  AFTER INSERT OR UPDATE OF assignee_id ON public.subtasks` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 83 | `  FOR EACH ROW EXECUTE FUNCTION public.notify_subtask_assignment();` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 84 | `(linha em branco)` | Separa blocos de codigo para melhorar a leitura. |
