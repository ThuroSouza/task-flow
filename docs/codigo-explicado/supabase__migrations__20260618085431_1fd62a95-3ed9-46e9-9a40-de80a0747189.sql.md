# supabase/migrations/20260618085431_1fd62a95-3ed9-46e9-9a40-de80a0747189.sql

Tipo: Migration SQL do Supabase.

Este documento foi gerado para estudo e manutencao. Ele preserva o codigo original e explica linha por linha sem alterar o comportamento do sistema publicado.

| Linha | Codigo original | Explicacao |
| ---: | --- | --- |
| 1 | `(linha em branco)` | Separa blocos de codigo para melhorar a leitura. |
| 2 | `-- 1) board_preferences: restrict to authenticated role` | Comentario da migration; explica a intencao daquele trecho SQL. |
| 3 | `DROP POLICY IF EXISTS "own prefs" ON public.board_preferences;` | Remove objeto antigo para permitir recriacao ou limpeza controlada. |
| 4 | `CREATE POLICY "own prefs" ON public.board_preferences` | Cria uma regra de seguranca RLS para controlar acesso aos dados. |
| 5 | `  FOR ALL TO authenticated` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 6 | `  USING (auth.uid() = user_id)` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 7 | `  WITH CHECK (auth.uid() = user_id);` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 8 | `(linha em branco)` | Separa blocos de codigo para melhorar a leitura. |
| 9 | `-- 2) tasks insert: require created_by = auth.uid() (no NULL bypass)` | Comentario da migration; explica a intencao daquele trecho SQL. |
| 10 | `DROP POLICY IF EXISTS tasks_insert ON public.tasks;` | Remove objeto antigo para permitir recriacao ou limpeza controlada. |
| 11 | `CREATE POLICY tasks_insert ON public.tasks` | Cria uma regra de seguranca RLS para controlar acesso aos dados. |
| 12 | `  FOR INSERT TO authenticated` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 13 | `  WITH CHECK (created_by = auth.uid());` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 14 | `(linha em branco)` | Separa blocos de codigo para melhorar a leitura. |
| 15 | `-- 3) Lock down SECURITY DEFINER helper functions: revoke public/anon/authenticated EXECUTE` | Comentario da migration; explica a intencao daquele trecho SQL. |
| 16 | `-- These remain callable from RLS policies (which run as table owner) and from server-side admin code.` | Comentario da migration; explica a intencao daquele trecho SQL. |
| 17 | `REVOKE EXECUTE ON FUNCTION public.seed_user_kanban_defaults(uuid) FROM PUBLIC, anon, authenticated;` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 18 | `REVOKE EXECUTE ON FUNCTION public.handle_new_user() FROM PUBLIC, anon, authenticated;` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 19 | `REVOKE EXECUTE ON FUNCTION public.set_task_created_by() FROM PUBLIC, anon, authenticated;` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 20 | `REVOKE EXECUTE ON FUNCTION public.set_client_note_created_by() FROM PUBLIC, anon, authenticated;` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 21 | `REVOKE EXECUTE ON FUNCTION public.set_client_file_uploaded_by() FROM PUBLIC, anon, authenticated;` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 22 | `REVOKE EXECUTE ON FUNCTION public.set_created_by_owner() FROM PUBLIC, anon, authenticated;` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 23 | `REVOKE EXECUTE ON FUNCTION public.sync_task_interruptions_count() FROM PUBLIC, anon, authenticated;` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 24 | `-- has_role and can_view_task are required by RLS policies for authenticated users; keep grants.` | Comentario da migration; explica a intencao daquele trecho SQL. |
| 25 | `(linha em branco)` | Separa blocos de codigo para melhorar a leitura. |
