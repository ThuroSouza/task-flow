# supabase/migrations/20260617132148_35985ce0-878f-4a66-8f3d-2dc404f5cee2.sql

Tipo: Migration SQL do Supabase.

Este documento foi gerado para estudo e manutencao. Ele preserva o codigo original e explica linha por linha sem alterar o comportamento do sistema publicado.

| Linha | Codigo original | Explicacao |
| ---: | --- | --- |
| 1 | `(linha em branco)` | Separa blocos de codigo para melhorar a leitura. |
| 2 | `-- ============================================================` | Comentario da migration; explica a intencao daquele trecho SQL. |
| 3 | `-- Per-user kanban customization + soft-delete + orientation` | Comentario da migration; explica a intencao daquele trecho SQL. |
| 4 | `-- ============================================================` | Comentario da migration; explica a intencao daquele trecho SQL. |
| 5 | `(linha em branco)` | Separa blocos de codigo para melhorar a leitura. |
| 6 | `-- 1) Schema additions` | Comentario da migration; explica a intencao daquele trecho SQL. |
| 7 | `ALTER TABLE public.tasks ADD COLUMN IF NOT EXISTS deleted_at timestamptz;` | Altera uma tabela existente, normalmente adicionando campos, chaves ou politicas. |
| 8 | `ALTER TABLE public.tasks ADD COLUMN IF NOT EXISTS deleted_by uuid;` | Altera uma tabela existente, normalmente adicionando campos, chaves ou politicas. |
| 9 | `CREATE INDEX IF NOT EXISTS tasks_deleted_at_idx ON public.tasks(deleted_at);` | Cria indice para acelerar consultas frequentes. |
| 10 | `(linha em branco)` | Separa blocos de codigo para melhorar a leitura. |
| 11 | `ALTER TABLE public.board_preferences` | Altera uma tabela existente, normalmente adicionando campos, chaves ou politicas. |
| 12 | `  ADD COLUMN IF NOT EXISTS kanban_orientation text NOT NULL DEFAULT 'vertical';` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 13 | `(linha em branco)` | Separa blocos de codigo para melhorar a leitura. |
| 14 | `-- 2) Backfill NULL created_by on existing tags/statuses/columns to the first admin` | Comentario da migration; explica a intencao daquele trecho SQL. |
| 15 | `DO $$` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 16 | `DECLARE admin_id uuid;` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 17 | `BEGIN` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 18 | `  SELECT user_id INTO admin_id FROM public.user_roles WHERE role = 'admin' ORDER BY id LIMIT 1;` | Consulta dados ou valida uma condicao no banco. |
| 19 | `  IF admin_id IS NOT NULL THEN` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 20 | `    UPDATE public.task_tags     SET created_by = admin_id WHERE created_by IS NULL;` | Atualiza registros existentes no banco. |
| 21 | `    UPDATE public.task_statuses SET created_by = admin_id WHERE created_by IS NULL;` | Atualiza registros existentes no banco. |
| 22 | `    UPDATE public.kanban_columns SET created_by = admin_id WHERE created_by IS NULL;` | Atualiza registros existentes no banco. |
| 23 | `  END IF;` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 24 | `END $$;` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 25 | `(linha em branco)` | Separa blocos de codigo para melhorar a leitura. |
| 26 | `-- 3) Per-user RLS on task_tags` | Comentario da migration; explica a intencao daquele trecho SQL. |
| 27 | `DROP POLICY IF EXISTS "Auth read tags"  ON public.task_tags;` | Remove objeto antigo para permitir recriacao ou limpeza controlada. |
| 28 | `DROP POLICY IF EXISTS "Auth write tags" ON public.task_tags;` | Remove objeto antigo para permitir recriacao ou limpeza controlada. |
| 29 | `DROP POLICY IF EXISTS task_tags_all ON public.task_tags;` | Remove objeto antigo para permitir recriacao ou limpeza controlada. |
| 30 | `DROP POLICY IF EXISTS task_tags_select ON public.task_tags;` | Remove objeto antigo para permitir recriacao ou limpeza controlada. |
| 31 | `DROP POLICY IF EXISTS task_tags_modify ON public.task_tags;` | Remove objeto antigo para permitir recriacao ou limpeza controlada. |
| 32 | `(linha em branco)` | Separa blocos de codigo para melhorar a leitura. |
| 33 | `CREATE POLICY task_tags_select ON public.task_tags FOR SELECT TO authenticated` | Cria uma regra de seguranca RLS para controlar acesso aos dados. |
| 34 | `  USING (created_by = auth.uid());` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 35 | `CREATE POLICY task_tags_insert ON public.task_tags FOR INSERT TO authenticated` | Cria uma regra de seguranca RLS para controlar acesso aos dados. |
| 36 | `  WITH CHECK (created_by = auth.uid());` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 37 | `CREATE POLICY task_tags_update ON public.task_tags FOR UPDATE TO authenticated` | Cria uma regra de seguranca RLS para controlar acesso aos dados. |
| 38 | `  USING (created_by = auth.uid()) WITH CHECK (created_by = auth.uid());` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 39 | `CREATE POLICY task_tags_delete ON public.task_tags FOR DELETE TO authenticated` | Cria uma regra de seguranca RLS para controlar acesso aos dados. |
| 40 | `  USING (created_by = auth.uid());` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 41 | `(linha em branco)` | Separa blocos de codigo para melhorar a leitura. |
| 42 | `-- 4) Per-user RLS on task_statuses` | Comentario da migration; explica a intencao daquele trecho SQL. |
| 43 | `DROP POLICY IF EXISTS "Auth read statuses"  ON public.task_statuses;` | Remove objeto antigo para permitir recriacao ou limpeza controlada. |
| 44 | `DROP POLICY IF EXISTS "Auth write statuses" ON public.task_statuses;` | Remove objeto antigo para permitir recriacao ou limpeza controlada. |
| 45 | `DROP POLICY IF EXISTS task_statuses_all ON public.task_statuses;` | Remove objeto antigo para permitir recriacao ou limpeza controlada. |
| 46 | `DROP POLICY IF EXISTS task_statuses_select ON public.task_statuses;` | Remove objeto antigo para permitir recriacao ou limpeza controlada. |
| 47 | `(linha em branco)` | Separa blocos de codigo para melhorar a leitura. |
| 48 | `CREATE POLICY task_statuses_select ON public.task_statuses FOR SELECT TO authenticated` | Cria uma regra de seguranca RLS para controlar acesso aos dados. |
| 49 | `  USING (created_by = auth.uid());` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 50 | `CREATE POLICY task_statuses_insert ON public.task_statuses FOR INSERT TO authenticated` | Cria uma regra de seguranca RLS para controlar acesso aos dados. |
| 51 | `  WITH CHECK (created_by = auth.uid());` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 52 | `CREATE POLICY task_statuses_update ON public.task_statuses FOR UPDATE TO authenticated` | Cria uma regra de seguranca RLS para controlar acesso aos dados. |
| 53 | `  USING (created_by = auth.uid()) WITH CHECK (created_by = auth.uid());` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 54 | `CREATE POLICY task_statuses_delete ON public.task_statuses FOR DELETE TO authenticated` | Cria uma regra de seguranca RLS para controlar acesso aos dados. |
| 55 | `  USING (created_by = auth.uid());` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 56 | `(linha em branco)` | Separa blocos de codigo para melhorar a leitura. |
| 57 | `-- 5) Per-user RLS on kanban_columns` | Comentario da migration; explica a intencao daquele trecho SQL. |
| 58 | `DROP POLICY IF EXISTS "Auth read columns"  ON public.kanban_columns;` | Remove objeto antigo para permitir recriacao ou limpeza controlada. |
| 59 | `DROP POLICY IF EXISTS "Auth write columns" ON public.kanban_columns;` | Remove objeto antigo para permitir recriacao ou limpeza controlada. |
| 60 | `DROP POLICY IF EXISTS kanban_columns_all ON public.kanban_columns;` | Remove objeto antigo para permitir recriacao ou limpeza controlada. |
| 61 | `DROP POLICY IF EXISTS kanban_columns_select ON public.kanban_columns;` | Remove objeto antigo para permitir recriacao ou limpeza controlada. |
| 62 | `(linha em branco)` | Separa blocos de codigo para melhorar a leitura. |
| 63 | `CREATE POLICY kanban_columns_select ON public.kanban_columns FOR SELECT TO authenticated` | Cria uma regra de seguranca RLS para controlar acesso aos dados. |
| 64 | `  USING (created_by = auth.uid());` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 65 | `CREATE POLICY kanban_columns_insert ON public.kanban_columns FOR INSERT TO authenticated` | Cria uma regra de seguranca RLS para controlar acesso aos dados. |
| 66 | `  WITH CHECK (created_by = auth.uid());` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 67 | `CREATE POLICY kanban_columns_update ON public.kanban_columns FOR UPDATE TO authenticated` | Cria uma regra de seguranca RLS para controlar acesso aos dados. |
| 68 | `  USING (created_by = auth.uid()) WITH CHECK (created_by = auth.uid());` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 69 | `CREATE POLICY kanban_columns_delete ON public.kanban_columns FOR DELETE TO authenticated` | Cria uma regra de seguranca RLS para controlar acesso aos dados. |
| 70 | `  USING (created_by = auth.uid());` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 71 | `(linha em branco)` | Separa blocos de codigo para melhorar a leitura. |
| 72 | `-- 6) Auto-set created_by on insert` | Comentario da migration; explica a intencao daquele trecho SQL. |
| 73 | `CREATE OR REPLACE FUNCTION public.set_created_by_owner()` | Define uma funcao no banco para reutilizar logica SQL. |
| 74 | `RETURNS trigger LANGUAGE plpgsql SET search_path = public AS $$` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 75 | `BEGIN` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 76 | `  IF NEW.created_by IS NULL THEN NEW.created_by := auth.uid(); END IF;` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 77 | `  RETURN NEW;` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 78 | `END $$;` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 79 | `(linha em branco)` | Separa blocos de codigo para melhorar a leitura. |
| 80 | `DROP TRIGGER IF EXISTS task_tags_set_owner ON public.task_tags;` | Remove objeto antigo para permitir recriacao ou limpeza controlada. |
| 81 | `CREATE TRIGGER task_tags_set_owner BEFORE INSERT ON public.task_tags` | Cria gatilho que executa uma funcao automaticamente em eventos da tabela. |
| 82 | `  FOR EACH ROW EXECUTE FUNCTION public.set_created_by_owner();` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 83 | `(linha em branco)` | Separa blocos de codigo para melhorar a leitura. |
| 84 | `DROP TRIGGER IF EXISTS task_statuses_set_owner ON public.task_statuses;` | Remove objeto antigo para permitir recriacao ou limpeza controlada. |
| 85 | `CREATE TRIGGER task_statuses_set_owner BEFORE INSERT ON public.task_statuses` | Cria gatilho que executa uma funcao automaticamente em eventos da tabela. |
| 86 | `  FOR EACH ROW EXECUTE FUNCTION public.set_created_by_owner();` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 87 | `(linha em branco)` | Separa blocos de codigo para melhorar a leitura. |
| 88 | `DROP TRIGGER IF EXISTS kanban_columns_set_owner ON public.kanban_columns;` | Remove objeto antigo para permitir recriacao ou limpeza controlada. |
| 89 | `CREATE TRIGGER kanban_columns_set_owner BEFORE INSERT ON public.kanban_columns` | Cria gatilho que executa uma funcao automaticamente em eventos da tabela. |
| 90 | `  FOR EACH ROW EXECUTE FUNCTION public.set_created_by_owner();` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 91 | `(linha em branco)` | Separa blocos de codigo para melhorar a leitura. |
| 92 | `-- 7) Seed defaults for a user (idempotent)` | Comentario da migration; explica a intencao daquele trecho SQL. |
| 93 | `CREATE OR REPLACE FUNCTION public.seed_user_kanban_defaults(_user_id uuid)` | Define uma funcao no banco para reutilizar logica SQL. |
| 94 | `RETURNS void LANGUAGE plpgsql SECURITY DEFINER SET search_path = public AS $$` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 95 | `BEGIN` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 96 | `  IF NOT EXISTS (SELECT 1 FROM public.task_statuses WHERE created_by = _user_id) THEN` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 97 | `    INSERT INTO public.task_statuses (name, color, position, is_completed, is_active, created_by) VALUES` | Insere dados iniciais ou registros de apoio. |
| 98 | `      ('Em andamento', '#3b82f6', 0, false, true, _user_id),` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 99 | `      ('Concluída',    '#10b981', 1, true,  true, _user_id);` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 100 | `  END IF;` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 101 | `(linha em branco)` | Separa blocos de codigo para melhorar a leitura. |
| 102 | `  IF NOT EXISTS (SELECT 1 FROM public.kanban_columns WHERE created_by = _user_id) THEN` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 103 | `    INSERT INTO public.kanban_columns (name, color, position, created_by) VALUES` | Insere dados iniciais ou registros de apoio. |
| 104 | `      ('A fazer',  '#64748b', 0, _user_id),` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 105 | `      ('Concluída','#10b981', 1, _user_id);` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 106 | `  END IF;` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 107 | `END $$;` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 108 | `(linha em branco)` | Separa blocos de codigo para melhorar a leitura. |
| 109 | `-- 8) Hook into new-user creation` | Comentario da migration; explica a intencao daquele trecho SQL. |
| 110 | `CREATE OR REPLACE FUNCTION public.handle_new_user()` | Define uma funcao no banco para reutilizar logica SQL. |
| 111 | `RETURNS trigger LANGUAGE plpgsql SECURITY DEFINER SET search_path = public AS $$` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 112 | `DECLARE user_count INT;` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 113 | `BEGIN` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 114 | `  INSERT INTO public.profiles (id, full_name, email)` | Insere dados iniciais ou registros de apoio. |
| 115 | `  VALUES (NEW.id, COALESCE(NEW.raw_user_meta_data->>'full_name', NEW.raw_user_meta_data->>'name', split_part(NEW.email, '@', 1)), NEW.email);` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 116 | `(linha em branco)` | Separa blocos de codigo para melhorar a leitura. |
| 117 | `  SELECT COUNT(*) INTO user_count FROM public.profiles;` | Consulta dados ou valida uma condicao no banco. |
| 118 | `  IF user_count = 1 THEN` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 119 | `    INSERT INTO public.user_roles (user_id, role) VALUES (NEW.id, 'admin');` | Insere dados iniciais ou registros de apoio. |
| 120 | `  ELSE` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 121 | `    INSERT INTO public.user_roles (user_id, role) VALUES (NEW.id, 'member');` | Insere dados iniciais ou registros de apoio. |
| 122 | `  END IF;` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 123 | `(linha em branco)` | Separa blocos de codigo para melhorar a leitura. |
| 124 | `  PERFORM public.seed_user_kanban_defaults(NEW.id);` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 125 | `  RETURN NEW;` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 126 | `END $$;` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 127 | `(linha em branco)` | Separa blocos de codigo para melhorar a leitura. |
| 128 | `-- 9) Backfill defaults for any existing profile that lacks them` | Comentario da migration; explica a intencao daquele trecho SQL. |
| 129 | `DO $$` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 130 | `DECLARE p_id uuid;` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 131 | `BEGIN` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 132 | `  FOR p_id IN SELECT id FROM public.profiles LOOP` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 133 | `    PERFORM public.seed_user_kanban_defaults(p_id);` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 134 | `  END LOOP;` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 135 | `END $$;` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 136 | `(linha em branco)` | Separa blocos de codigo para melhorar a leitura. |
