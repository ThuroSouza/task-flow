# supabase/migrations/20260617133359_8bab5a5c-36ad-4f2f-a139-ee3a77017902.sql

Tipo: Migration SQL do Supabase.

Este documento foi gerado para estudo e manutencao. Ele preserva o codigo original e explica linha por linha sem alterar o comportamento do sistema publicado.

| Linha | Codigo original | Explicacao |
| ---: | --- | --- |
| 1 | `(linha em branco)` | Separa blocos de codigo para melhorar a leitura. |
| 2 | `-- ============================================================` | Comentario da migration; explica a intencao daquele trecho SQL. |
| 3 | `-- Fix duplicate seeding + lock the built-in "Concluída" column` | Comentario da migration; explica a intencao daquele trecho SQL. |
| 4 | `-- The kanban UI already renders a fixed green "Concluída" drop zone` | Comentario da migration; explica a intencao daquele trecho SQL. |
| 5 | `-- (CompletedColumn), so we must NOT also store a "Concluída" row in` | Comentario da migration; explica a intencao daquele trecho SQL. |
| 6 | `-- public.kanban_columns — that produced visible duplicates.` | Comentario da migration; explica a intencao daquele trecho SQL. |
| 7 | `-- ============================================================` | Comentario da migration; explica a intencao daquele trecho SQL. |
| 8 | `(linha em branco)` | Separa blocos de codigo para melhorar a leitura. |
| 9 | `-- 1) Remove "Concluída/Concluído" rows from kanban_columns for everyone.` | Comentario da migration; explica a intencao daquele trecho SQL. |
| 10 | `--    Move any tasks currently parked there to NULL column (they still` | Comentario da migration; explica a intencao daquele trecho SQL. |
| 11 | `--    show up in the green completed area via completed_at/status).` | Comentario da migration; explica a intencao daquele trecho SQL. |
| 12 | `UPDATE public.tasks` | Atualiza registros existentes no banco. |
| 13 | `SET column_id = NULL` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 14 | `WHERE column_id IN (` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 15 | `  SELECT id FROM public.kanban_columns` | Consulta dados ou valida uma condicao no banco. |
| 16 | `  WHERE lower(trim(name)) IN ('concluída','concluida','concluído','concluido')` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 17 | `);` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 18 | `(linha em branco)` | Separa blocos de codigo para melhorar a leitura. |
| 19 | `DELETE FROM public.kanban_columns` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 20 | `WHERE lower(trim(name)) IN ('concluída','concluida','concluído','concluido');` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 21 | `(linha em branco)` | Separa blocos de codigo para melhorar a leitura. |
| 22 | `-- 2) Deduplicate per-user same-name columns (keep the earliest).` | Comentario da migration; explica a intencao daquele trecho SQL. |
| 23 | `WITH dups AS (` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 24 | `  SELECT id,` | Consulta dados ou valida uma condicao no banco. |
| 25 | `         ROW_NUMBER() OVER (` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 26 | `           PARTITION BY created_by, lower(trim(name))` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 27 | `           ORDER BY created_at NULLS LAST, id` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 28 | `         ) AS rn` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 29 | `  FROM public.kanban_columns` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 30 | `)` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 31 | `DELETE FROM public.kanban_columns` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 32 | `WHERE id IN (SELECT id FROM dups WHERE rn > 1);` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 33 | `(linha em branco)` | Separa blocos de codigo para melhorar a leitura. |
| 34 | `-- 3) Rewrite the seed: only "A fazer". Concluída is rendered by the UI` | Comentario da migration; explica a intencao daquele trecho SQL. |
| 35 | `--    as a built-in zone and never lives in kanban_columns anymore.` | Comentario da migration; explica a intencao daquele trecho SQL. |
| 36 | `CREATE OR REPLACE FUNCTION public.seed_user_kanban_defaults(_user_id uuid)` | Define uma funcao no banco para reutilizar logica SQL. |
| 37 | `RETURNS void` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 38 | `LANGUAGE plpgsql` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 39 | `SECURITY DEFINER` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 40 | `SET search_path = public` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 41 | `AS $$` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 42 | `BEGIN` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 43 | `  IF NOT EXISTS (` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 44 | `    SELECT 1 FROM public.task_statuses WHERE created_by = _user_id` | Consulta dados ou valida uma condicao no banco. |
| 45 | `  ) THEN` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 46 | `    INSERT INTO public.task_statuses` | Insere dados iniciais ou registros de apoio. |
| 47 | `      (name, color, position, is_completed, is_active, created_by)` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 48 | `    VALUES` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 49 | `      ('Em andamento', '#3b82f6', 0, false, true,  _user_id),` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 50 | `      ('Concluída',    '#10b981', 1, true,  false, _user_id);` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 51 | `  END IF;` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 52 | `(linha em branco)` | Separa blocos de codigo para melhorar a leitura. |
| 53 | `  IF NOT EXISTS (` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 54 | `    SELECT 1 FROM public.kanban_columns WHERE created_by = _user_id` | Consulta dados ou valida uma condicao no banco. |
| 55 | `  ) THEN` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 56 | `    INSERT INTO public.kanban_columns` | Insere dados iniciais ou registros de apoio. |
| 57 | `      (name, color, position, created_by)` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 58 | `    VALUES` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 59 | `      ('A fazer', '#64748b', 0, _user_id);` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 60 | `  END IF;` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 61 | `END` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 62 | `$$;` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 63 | `(linha em branco)` | Separa blocos de codigo para melhorar a leitura. |
