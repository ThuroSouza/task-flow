# supabase/migrations/20260610215950_8791c1ff-151d-4872-8431-b442582b48e1.sql

Tipo: Migration SQL do Supabase.

Este documento foi gerado para estudo e manutencao. Ele preserva o codigo original e explica linha por linha sem alterar o comportamento do sistema publicado.

| Linha | Codigo original | Explicacao |
| ---: | --- | --- |
| 1 | `(linha em branco)` | Separa blocos de codigo para melhorar a leitura. |
| 2 | `CREATE TABLE public.task_tag_links (` | Cria uma tabela no banco de dados Supabase/PostgreSQL. |
| 3 | `  task_id uuid NOT NULL REFERENCES public.tasks(id) ON DELETE CASCADE,` | Define relacionamento entre tabelas por chave estrangeira. |
| 4 | `  tag_id uuid NOT NULL REFERENCES public.task_tags(id) ON DELETE CASCADE,` | Define relacionamento entre tabelas por chave estrangeira. |
| 5 | `  created_at timestamptz NOT NULL DEFAULT now(),` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 6 | `  PRIMARY KEY (task_id, tag_id)` | Define identificador unico principal do registro. |
| 7 | `);` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 8 | `CREATE INDEX task_tag_links_task_idx ON public.task_tag_links(task_id);` | Cria indice para acelerar consultas frequentes. |
| 9 | `CREATE INDEX task_tag_links_tag_idx ON public.task_tag_links(tag_id);` | Cria indice para acelerar consultas frequentes. |
| 10 | `GRANT SELECT, INSERT, UPDATE, DELETE ON public.task_tag_links TO authenticated;` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 11 | `GRANT ALL ON public.task_tag_links TO service_role;` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 12 | `ALTER TABLE public.task_tag_links ENABLE ROW LEVEL SECURITY;` | Altera uma tabela existente, normalmente adicionando campos, chaves ou politicas. |
| 13 | `CREATE POLICY "task_tag_links_all_auth" ON public.task_tag_links TO authenticated USING (true) WITH CHECK (true);` | Cria uma regra de seguranca RLS para controlar acesso aos dados. |
| 14 | `(linha em branco)` | Separa blocos de codigo para melhorar a leitura. |
| 15 | `INSERT INTO public.task_tag_links (task_id, tag_id)` | Insere dados iniciais ou registros de apoio. |
| 16 | `SELECT id, tag_id FROM public.tasks WHERE tag_id IS NOT NULL` | Consulta dados ou valida uma condicao no banco. |
| 17 | `ON CONFLICT DO NOTHING;` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 18 | `(linha em branco)` | Separa blocos de codigo para melhorar a leitura. |
