# supabase/migrations/20260617124729_e845c780-3c8b-4035-b7b6-ce51296cc9a9.sql

Tipo: Migration SQL do Supabase.

Este documento foi gerado para estudo e manutencao. Ele preserva o codigo original e explica linha por linha sem alterar o comportamento do sistema publicado.

| Linha | Codigo original | Explicacao |
| ---: | --- | --- |
| 1 | `(linha em branco)` | Separa blocos de codigo para melhorar a leitura. |
| 2 | `-- 1) Storage: restrict DELETE on task-attachments to the uploader (via attachments.uploaded_by)` | Comentario da migration; explica a intencao daquele trecho SQL. |
| 3 | `DROP POLICY IF EXISTS "auth_can_delete_attachments" ON storage.objects;` | Remove objeto antigo para permitir recriacao ou limpeza controlada. |
| 4 | `CREATE POLICY "task_attachments_delete_own"` | Cria uma regra de seguranca RLS para controlar acesso aos dados. |
| 5 | `ON storage.objects FOR DELETE TO authenticated` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 6 | `USING (` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 7 | `  bucket_id = 'task-attachments'` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 8 | `  AND EXISTS (` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 9 | `    SELECT 1 FROM public.attachments a` | Consulta dados ou valida uma condicao no banco. |
| 10 | `    WHERE a.storage_path = storage.objects.name` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 11 | `      AND a.uploaded_by = auth.uid()` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 12 | `  )` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 13 | `);` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 14 | `(linha em branco)` | Separa blocos de codigo para melhorar a leitura. |
| 15 | `-- Keep SELECT/INSERT broad to preserve team-collaboration behavior on shared tasks,` | Comentario da migration; explica a intencao daquele trecho SQL. |
| 16 | `-- but ensure INSERT is owned by the uploader (path-agnostic; metadata table tracks ownership).` | Comentario da migration; explica a intencao daquele trecho SQL. |
| 17 | `DROP POLICY IF EXISTS "auth_can_select_attachments" ON storage.objects;` | Remove objeto antigo para permitir recriacao ou limpeza controlada. |
| 18 | `CREATE POLICY "task_attachments_select_auth"` | Cria uma regra de seguranca RLS para controlar acesso aos dados. |
| 19 | `ON storage.objects FOR SELECT TO authenticated` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 20 | `USING (bucket_id = 'task-attachments');` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 21 | `(linha em branco)` | Separa blocos de codigo para melhorar a leitura. |
| 22 | `DROP POLICY IF EXISTS "auth_can_upload_attachments" ON storage.objects;` | Remove objeto antigo para permitir recriacao ou limpeza controlada. |
| 23 | `CREATE POLICY "task_attachments_insert_auth"` | Cria uma regra de seguranca RLS para controlar acesso aos dados. |
| 24 | `ON storage.objects FOR INSERT TO authenticated` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 25 | `WITH CHECK (bucket_id = 'task-attachments' AND auth.uid() = owner);` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 26 | `(linha em branco)` | Separa blocos de codigo para melhorar a leitura. |
| 27 | `-- 2) Revoke EXECUTE on SECURITY DEFINER trigger functions (only triggers call these)` | Comentario da migration; explica a intencao daquele trecho SQL. |
| 28 | `REVOKE EXECUTE ON FUNCTION public.handle_new_user() FROM PUBLIC, anon, authenticated;` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 29 | `REVOKE EXECUTE ON FUNCTION public.sync_task_interruptions_count() FROM PUBLIC, anon, authenticated;` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 30 | `REVOKE EXECUTE ON FUNCTION public.set_updated_at() FROM PUBLIC, anon, authenticated;` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 31 | `-- Note: public.has_role(uuid, app_role) must remain executable by authenticated because RLS policies invoke it.` | Comentario da migration; explica a intencao daquele trecho SQL. |
| 32 | `(linha em branco)` | Separa blocos de codigo para melhorar a leitura. |
