# supabase/migrations/20260616140039_1c3ace74-853c-4834-9c56-bc980888d9eb.sql

Tipo: Migration SQL do Supabase.

Este documento foi gerado para estudo e manutencao. Ele preserva o codigo original e explica linha por linha sem alterar o comportamento do sistema publicado.

| Linha | Codigo original | Explicacao |
| ---: | --- | --- |
| 1 | `(linha em branco)` | Separa blocos de codigo para melhorar a leitura. |
| 2 | `-- Board preferences (per user)` | Comentario da migration; explica a intencao daquele trecho SQL. |
| 3 | `CREATE TABLE public.board_preferences (` | Cria uma tabela no banco de dados Supabase/PostgreSQL. |
| 4 | `  id uuid PRIMARY KEY DEFAULT gen_random_uuid(),` | Define identificador unico principal do registro. |
| 5 | `  user_id uuid NOT NULL UNIQUE REFERENCES auth.users(id) ON DELETE CASCADE,` | Define relacionamento entre tabelas por chave estrangeira. |
| 6 | `  field_order jsonb NOT NULL DEFAULT '["tags","description","assignee","priority","status","due","subtasks","comments","attachments","interruptions"]'::jsonb,` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 7 | `  hidden_fields jsonb NOT NULL DEFAULT '[]'::jsonb,` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 8 | `  interruption_color text NOT NULL DEFAULT '#ef4444',` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 9 | `  created_at timestamptz NOT NULL DEFAULT now(),` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 10 | `  updated_at timestamptz NOT NULL DEFAULT now()` | Atualiza registros existentes no banco. |
| 11 | `);` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 12 | `GRANT SELECT, INSERT, UPDATE, DELETE ON public.board_preferences TO authenticated;` | Define relacionamento entre tabelas por chave estrangeira. |
| 13 | `GRANT ALL ON public.board_preferences TO service_role;` | Define relacionamento entre tabelas por chave estrangeira. |
| 14 | `ALTER TABLE public.board_preferences ENABLE ROW LEVEL SECURITY;` | Altera uma tabela existente, normalmente adicionando campos, chaves ou politicas. |
| 15 | `CREATE POLICY "own prefs" ON public.board_preferences FOR ALL USING (auth.uid() = user_id) WITH CHECK (auth.uid() = user_id);` | Cria uma regra de seguranca RLS para controlar acesso aos dados. |
| 16 | `CREATE TRIGGER board_preferences_updated_at BEFORE UPDATE ON public.board_preferences FOR EACH ROW EXECUTE FUNCTION public.set_updated_at();` | Cria gatilho que executa uma funcao automaticamente em eventos da tabela. |
| 17 | `(linha em branco)` | Separa blocos de codigo para melhorar a leitura. |
| 18 | `-- Task interruptions log` | Comentario da migration; explica a intencao daquele trecho SQL. |
| 19 | `CREATE TABLE public.task_interruptions (` | Cria uma tabela no banco de dados Supabase/PostgreSQL. |
| 20 | `  id uuid PRIMARY KEY DEFAULT gen_random_uuid(),` | Define identificador unico principal do registro. |
| 21 | `  task_id uuid NOT NULL REFERENCES public.tasks(id) ON DELETE CASCADE,` | Define relacionamento entre tabelas por chave estrangeira. |
| 22 | `  user_id uuid NOT NULL REFERENCES auth.users(id) ON DELETE CASCADE,` | Define relacionamento entre tabelas por chave estrangeira. |
| 23 | `  reason text NOT NULL DEFAULT '',` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 24 | `  created_at timestamptz NOT NULL DEFAULT now(),` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 25 | `  updated_at timestamptz NOT NULL DEFAULT now()` | Atualiza registros existentes no banco. |
| 26 | `);` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 27 | `GRANT SELECT, INSERT, UPDATE, DELETE ON public.task_interruptions TO authenticated;` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 28 | `GRANT ALL ON public.task_interruptions TO service_role;` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 29 | `ALTER TABLE public.task_interruptions ENABLE ROW LEVEL SECURITY;` | Altera uma tabela existente, normalmente adicionando campos, chaves ou politicas. |
| 30 | `CREATE POLICY "auth interruptions" ON public.task_interruptions FOR ALL TO authenticated USING (true) WITH CHECK (auth.uid() = user_id);` | Cria uma regra de seguranca RLS para controlar acesso aos dados. |
| 31 | `CREATE INDEX task_interruptions_task_id_idx ON public.task_interruptions(task_id);` | Cria indice para acelerar consultas frequentes. |
| 32 | `CREATE TRIGGER task_interruptions_updated_at BEFORE UPDATE ON public.task_interruptions FOR EACH ROW EXECUTE FUNCTION public.set_updated_at();` | Cria gatilho que executa uma funcao automaticamente em eventos da tabela. |
| 33 | `(linha em branco)` | Separa blocos de codigo para melhorar a leitura. |
| 34 | `-- Keep tasks.interruptions in sync as a cached count` | Comentario da migration; explica a intencao daquele trecho SQL. |
| 35 | `CREATE OR REPLACE FUNCTION public.sync_task_interruptions_count()` | Define uma funcao no banco para reutilizar logica SQL. |
| 36 | `RETURNS trigger LANGUAGE plpgsql SECURITY DEFINER SET search_path = public AS $$` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 37 | `DECLARE tid uuid;` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 38 | `BEGIN` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 39 | `  tid := COALESCE(NEW.task_id, OLD.task_id);` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 40 | `  UPDATE public.tasks SET interruptions = (SELECT count(*) FROM public.task_interruptions WHERE task_id = tid) WHERE id = tid;` | Atualiza registros existentes no banco. |
| 41 | `  RETURN NULL;` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 42 | `END; $$;` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 43 | `CREATE TRIGGER task_interruptions_count_aiud` | Cria gatilho que executa uma funcao automaticamente em eventos da tabela. |
| 44 | `AFTER INSERT OR DELETE OR UPDATE ON public.task_interruptions` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 45 | `FOR EACH ROW EXECUTE FUNCTION public.sync_task_interruptions_count();` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 46 | `(linha em branco)` | Separa blocos de codigo para melhorar a leitura. |
