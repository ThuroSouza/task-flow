# supabase/migrations/20260617125908_d4d00766-b32d-4e5d-9a14-3f4be2d73488.sql

Tipo: Migration SQL do Supabase.

Este documento foi gerado para estudo e manutencao. Ele preserva o codigo original e explica linha por linha sem alterar o comportamento do sistema publicado.

| Linha | Codigo original | Explicacao |
| ---: | --- | --- |
| 1 | `(linha em branco)` | Separa blocos de codigo para melhorar a leitura. |
| 2 | `-- 1. Soft-delete flag em profiles` | Comentario da migration; explica a intencao daquele trecho SQL. |
| 3 | `ALTER TABLE public.profiles ADD COLUMN IF NOT EXISTS is_active boolean NOT NULL DEFAULT true;` | Altera uma tabela existente, normalmente adicionando campos, chaves ou politicas. |
| 4 | `(linha em branco)` | Separa blocos de codigo para melhorar a leitura. |
| 5 | `-- 2. Helper: tarefa visível ao usuário corrente?` | Comentario da migration; explica a intencao daquele trecho SQL. |
| 6 | `CREATE OR REPLACE FUNCTION public.can_view_task(_task_id uuid)` | Define uma funcao no banco para reutilizar logica SQL. |
| 7 | `RETURNS boolean` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 8 | `LANGUAGE sql` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 9 | `STABLE SECURITY DEFINER` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 10 | `SET search_path = public` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 11 | `AS $$` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 12 | `  SELECT EXISTS (` | Consulta dados ou valida uma condicao no banco. |
| 13 | `    SELECT 1 FROM public.tasks t` | Consulta dados ou valida uma condicao no banco. |
| 14 | `    WHERE t.id = _task_id` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 15 | `      AND (` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 16 | `        public.has_role(auth.uid(), 'admin'::app_role)` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 17 | `        OR t.assignee_id = auth.uid()` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 18 | `        OR t.created_by = auth.uid()` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 19 | `      )` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 20 | `  )` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 21 | `$$;` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 22 | `REVOKE EXECUTE ON FUNCTION public.can_view_task(uuid) FROM PUBLIC, anon;` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 23 | `GRANT EXECUTE ON FUNCTION public.can_view_task(uuid) TO authenticated;` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 24 | `(linha em branco)` | Separa blocos de codigo para melhorar a leitura. |
| 25 | `-- 3. Default created_by em tasks` | Comentario da migration; explica a intencao daquele trecho SQL. |
| 26 | `CREATE OR REPLACE FUNCTION public.set_task_created_by()` | Define uma funcao no banco para reutilizar logica SQL. |
| 27 | `RETURNS trigger` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 28 | `LANGUAGE plpgsql` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 29 | `SET search_path = public` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 30 | `AS $$` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 31 | `BEGIN` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 32 | `  IF NEW.created_by IS NULL THEN NEW.created_by := auth.uid(); END IF;` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 33 | `  RETURN NEW;` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 34 | `END $$;` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 35 | `DROP TRIGGER IF EXISTS trg_tasks_default_created_by ON public.tasks;` | Remove objeto antigo para permitir recriacao ou limpeza controlada. |
| 36 | `CREATE TRIGGER trg_tasks_default_created_by` | Cria gatilho que executa uma funcao automaticamente em eventos da tabela. |
| 37 | `  BEFORE INSERT ON public.tasks` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 38 | `  FOR EACH ROW EXECUTE FUNCTION public.set_task_created_by();` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 39 | `(linha em branco)` | Separa blocos de codigo para melhorar a leitura. |
| 40 | `-- 4. TASKS: política por papel` | Comentario da migration; explica a intencao daquele trecho SQL. |
| 41 | `DROP POLICY IF EXISTS tasks_all_auth ON public.tasks;` | Remove objeto antigo para permitir recriacao ou limpeza controlada. |
| 42 | `CREATE POLICY tasks_select ON public.tasks FOR SELECT TO authenticated` | Cria uma regra de seguranca RLS para controlar acesso aos dados. |
| 43 | `  USING (public.has_role(auth.uid(), 'admin'::app_role) OR assignee_id = auth.uid() OR created_by = auth.uid());` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 44 | `CREATE POLICY tasks_insert ON public.tasks FOR INSERT TO authenticated` | Cria uma regra de seguranca RLS para controlar acesso aos dados. |
| 45 | `  WITH CHECK (created_by IS NULL OR created_by = auth.uid() OR public.has_role(auth.uid(), 'admin'::app_role));` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 46 | `CREATE POLICY tasks_update ON public.tasks FOR UPDATE TO authenticated` | Cria uma regra de seguranca RLS para controlar acesso aos dados. |
| 47 | `  USING (public.has_role(auth.uid(), 'admin'::app_role) OR assignee_id = auth.uid() OR created_by = auth.uid())` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 48 | `  WITH CHECK (public.has_role(auth.uid(), 'admin'::app_role) OR assignee_id = auth.uid() OR created_by = auth.uid());` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 49 | `CREATE POLICY tasks_delete ON public.tasks FOR DELETE TO authenticated` | Cria uma regra de seguranca RLS para controlar acesso aos dados. |
| 50 | `  USING (public.has_role(auth.uid(), 'admin'::app_role) OR created_by = auth.uid());` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 51 | `(linha em branco)` | Separa blocos de codigo para melhorar a leitura. |
| 52 | `-- 5. SUBTASKS / ATTACHMENTS / TAG LINKS / INTERRUPTIONS: gated by parent task` | Comentario da migration; explica a intencao daquele trecho SQL. |
| 53 | `DROP POLICY IF EXISTS subtasks_all_auth ON public.subtasks;` | Remove objeto antigo para permitir recriacao ou limpeza controlada. |
| 54 | `CREATE POLICY subtasks_all ON public.subtasks FOR ALL TO authenticated` | Cria uma regra de seguranca RLS para controlar acesso aos dados. |
| 55 | `  USING (public.can_view_task(task_id)) WITH CHECK (public.can_view_task(task_id));` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 56 | `(linha em branco)` | Separa blocos de codigo para melhorar a leitura. |
| 57 | `DROP POLICY IF EXISTS att_all_auth ON public.attachments;` | Remove objeto antigo para permitir recriacao ou limpeza controlada. |
| 58 | `CREATE POLICY attachments_all ON public.attachments FOR ALL TO authenticated` | Cria uma regra de seguranca RLS para controlar acesso aos dados. |
| 59 | `  USING (public.can_view_task(task_id)) WITH CHECK (public.can_view_task(task_id));` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 60 | `(linha em branco)` | Separa blocos de codigo para melhorar a leitura. |
| 61 | `DROP POLICY IF EXISTS task_tag_links_all_auth ON public.task_tag_links;` | Remove objeto antigo para permitir recriacao ou limpeza controlada. |
| 62 | `CREATE POLICY task_tag_links_all ON public.task_tag_links FOR ALL TO authenticated` | Cria uma regra de seguranca RLS para controlar acesso aos dados. |
| 63 | `  USING (public.can_view_task(task_id)) WITH CHECK (public.can_view_task(task_id));` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 64 | `(linha em branco)` | Separa blocos de codigo para melhorar a leitura. |
| 65 | `DROP POLICY IF EXISTS "auth interruptions" ON public.task_interruptions;` | Remove objeto antigo para permitir recriacao ou limpeza controlada. |
| 66 | `CREATE POLICY task_interruptions_select ON public.task_interruptions FOR SELECT TO authenticated` | Cria uma regra de seguranca RLS para controlar acesso aos dados. |
| 67 | `  USING (public.can_view_task(task_id));` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 68 | `CREATE POLICY task_interruptions_write ON public.task_interruptions FOR INSERT TO authenticated` | Cria uma regra de seguranca RLS para controlar acesso aos dados. |
| 69 | `  WITH CHECK (auth.uid() = user_id AND public.can_view_task(task_id));` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 70 | `CREATE POLICY task_interruptions_update ON public.task_interruptions FOR UPDATE TO authenticated` | Cria uma regra de seguranca RLS para controlar acesso aos dados. |
| 71 | `  USING (auth.uid() = user_id) WITH CHECK (auth.uid() = user_id);` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 72 | `CREATE POLICY task_interruptions_delete ON public.task_interruptions FOR DELETE TO authenticated` | Cria uma regra de seguranca RLS para controlar acesso aos dados. |
| 73 | `  USING (auth.uid() = user_id OR public.has_role(auth.uid(), 'admin'::app_role));` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 74 | `(linha em branco)` | Separa blocos de codigo para melhorar a leitura. |
| 75 | `-- 6. COMMENTS / HISTORY: gated select by parent task` | Comentario da migration; explica a intencao daquele trecho SQL. |
| 76 | `DROP POLICY IF EXISTS comments_select_all ON public.comments;` | Remove objeto antigo para permitir recriacao ou limpeza controlada. |
| 77 | `CREATE POLICY comments_select ON public.comments FOR SELECT TO authenticated` | Cria uma regra de seguranca RLS para controlar acesso aos dados. |
| 78 | `  USING (public.can_view_task(task_id));` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 79 | `DROP POLICY IF EXISTS hist_select_all ON public.task_history;` | Remove objeto antigo para permitir recriacao ou limpeza controlada. |
| 80 | `CREATE POLICY hist_select ON public.task_history FOR SELECT TO authenticated` | Cria uma regra de seguranca RLS para controlar acesso aos dados. |
| 81 | `  USING (public.can_view_task(task_id));` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 82 | `(linha em branco)` | Separa blocos de codigo para melhorar a leitura. |
| 83 | `-- 7. PROFILES: admins podem atualizar qualquer perfil (incluindo is_active)` | Comentario da migration; explica a intencao daquele trecho SQL. |
| 84 | `DROP POLICY IF EXISTS profiles_update_own ON public.profiles;` | Remove objeto antigo para permitir recriacao ou limpeza controlada. |
| 85 | `CREATE POLICY profiles_update_own_or_admin ON public.profiles FOR UPDATE TO authenticated` | Cria uma regra de seguranca RLS para controlar acesso aos dados. |
| 86 | `  USING (auth.uid() = id OR public.has_role(auth.uid(), 'admin'::app_role))` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 87 | `  WITH CHECK (auth.uid() = id OR public.has_role(auth.uid(), 'admin'::app_role));` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 88 | `(linha em branco)` | Separa blocos de codigo para melhorar a leitura. |
