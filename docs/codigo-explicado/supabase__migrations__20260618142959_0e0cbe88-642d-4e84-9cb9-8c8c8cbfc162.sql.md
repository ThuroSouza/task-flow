# supabase/migrations/20260618142959_0e0cbe88-642d-4e84-9cb9-8c8c8cbfc162.sql

Tipo: Migration SQL do Supabase.

Este documento foi gerado para estudo e manutencao. Ele preserva o codigo original e explica linha por linha sem alterar o comportamento do sistema publicado.

| Linha | Codigo original | Explicacao |
| ---: | --- | --- |
| 1 | `(linha em branco)` | Separa blocos de codigo para melhorar a leitura. |
| 2 | `-- 1) Clients: restrict writes to admins, keep read open to authenticated` | Comentario da migration; explica a intencao daquele trecho SQL. |
| 3 | `DROP POLICY IF EXISTS clients_all_auth ON public.clients;` | Remove objeto antigo para permitir recriacao ou limpeza controlada. |
| 4 | `(linha em branco)` | Separa blocos de codigo para melhorar a leitura. |
| 5 | `CREATE POLICY clients_select_auth ON public.clients` | Cria uma regra de seguranca RLS para controlar acesso aos dados. |
| 6 | `  FOR SELECT TO authenticated USING (true);` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 7 | `(linha em branco)` | Separa blocos de codigo para melhorar a leitura. |
| 8 | `CREATE POLICY clients_insert_admin ON public.clients` | Cria uma regra de seguranca RLS para controlar acesso aos dados. |
| 9 | `  FOR INSERT TO authenticated` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 10 | `  WITH CHECK (public.has_role(auth.uid(), 'admin'::app_role));` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 11 | `(linha em branco)` | Separa blocos de codigo para melhorar a leitura. |
| 12 | `CREATE POLICY clients_update_admin ON public.clients` | Cria uma regra de seguranca RLS para controlar acesso aos dados. |
| 13 | `  FOR UPDATE TO authenticated` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 14 | `  USING (public.has_role(auth.uid(), 'admin'::app_role))` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 15 | `  WITH CHECK (public.has_role(auth.uid(), 'admin'::app_role));` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 16 | `(linha em branco)` | Separa blocos de codigo para melhorar a leitura. |
| 17 | `CREATE POLICY clients_delete_admin ON public.clients` | Cria uma regra de seguranca RLS para controlar acesso aos dados. |
| 18 | `  FOR DELETE TO authenticated` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 19 | `  USING (public.has_role(auth.uid(), 'admin'::app_role));` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 20 | `(linha em branco)` | Separa blocos de codigo para melhorar a leitura. |
| 21 | `-- 2) client_files: require uploaded_by = auth.uid() on insert` | Comentario da migration; explica a intencao daquele trecho SQL. |
| 22 | `DROP POLICY IF EXISTS client_files_insert ON public.client_files;` | Remove objeto antigo para permitir recriacao ou limpeza controlada. |
| 23 | `CREATE POLICY client_files_insert ON public.client_files` | Cria uma regra de seguranca RLS para controlar acesso aos dados. |
| 24 | `  FOR INSERT TO authenticated` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 25 | `  WITH CHECK (uploaded_by = auth.uid());` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 26 | `(linha em branco)` | Separa blocos de codigo para melhorar a leitura. |
| 27 | `-- 3) client_notes: require created_by = auth.uid() on insert` | Comentario da migration; explica a intencao daquele trecho SQL. |
| 28 | `DROP POLICY IF EXISTS client_notes_insert ON public.client_notes;` | Remove objeto antigo para permitir recriacao ou limpeza controlada. |
| 29 | `CREATE POLICY client_notes_insert ON public.client_notes` | Cria uma regra de seguranca RLS para controlar acesso aos dados. |
| 30 | `  FOR INSERT TO authenticated` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 31 | `  WITH CHECK (created_by = auth.uid());` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 32 | `(linha em branco)` | Separa blocos de codigo para melhorar a leitura. |
| 33 | `-- 4) Storage: restrict SELECT on task-attachments bucket to users that can view the related task` | Comentario da migration; explica a intencao daquele trecho SQL. |
| 34 | `DROP POLICY IF EXISTS task_attachments_select_auth ON storage.objects;` | Remove objeto antigo para permitir recriacao ou limpeza controlada. |
| 35 | `CREATE POLICY task_attachments_select_auth ON storage.objects` | Cria uma regra de seguranca RLS para controlar acesso aos dados. |
| 36 | `  FOR SELECT TO authenticated` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 37 | `  USING (` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 38 | `    bucket_id = 'task-attachments'` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 39 | `    AND EXISTS (` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 40 | `      SELECT 1` | Consulta dados ou valida uma condicao no banco. |
| 41 | `      FROM public.attachments a` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 42 | `      JOIN public.tasks t ON t.id = a.task_id` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 43 | `      WHERE a.storage_path = storage.objects.name` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 44 | `        AND (` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 45 | `          t.created_by = auth.uid()` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 46 | `          OR t.assignee_id = auth.uid()` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 47 | `          OR public.has_role(auth.uid(), 'admin'::app_role)` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 48 | `        )` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 49 | `    )` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 50 | `  );` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 51 | `(linha em branco)` | Separa blocos de codigo para melhorar a leitura. |
