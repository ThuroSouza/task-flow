# supabase/migrations/20260604160658_03ff13bb-141c-4626-9180-047dbb8a0df6.sql

Tipo: Migration SQL do Supabase.

Este documento foi gerado para estudo e manutencao. Ele preserva o codigo original e explica linha por linha sem alterar o comportamento do sistema publicado.

| Linha | Codigo original | Explicacao |
| ---: | --- | --- |
| 1 | `(linha em branco)` | Separa blocos de codigo para melhorar a leitura. |
| 2 | `CREATE POLICY "auth_can_select_attachments" ON storage.objects FOR SELECT TO authenticated USING (bucket_id = 'task-attachments');` | Cria uma regra de seguranca RLS para controlar acesso aos dados. |
| 3 | `CREATE POLICY "auth_can_upload_attachments" ON storage.objects FOR INSERT TO authenticated WITH CHECK (bucket_id = 'task-attachments');` | Cria uma regra de seguranca RLS para controlar acesso aos dados. |
| 4 | `CREATE POLICY "auth_can_delete_attachments" ON storage.objects FOR DELETE TO authenticated USING (bucket_id = 'task-attachments');` | Cria uma regra de seguranca RLS para controlar acesso aos dados. |
| 5 | `(linha em branco)` | Separa blocos de codigo para melhorar a leitura. |
