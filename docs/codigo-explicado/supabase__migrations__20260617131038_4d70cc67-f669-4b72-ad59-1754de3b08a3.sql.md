# supabase/migrations/20260617131038_4d70cc67-f669-4b72-ad59-1754de3b08a3.sql

Tipo: Migration SQL do Supabase.

Este documento foi gerado para estudo e manutencao. Ele preserva o codigo original e explica linha por linha sem alterar o comportamento do sistema publicado.

| Linha | Codigo original | Explicacao |
| ---: | --- | --- |
| 1 | `(linha em branco)` | Separa blocos de codigo para melhorar a leitura. |
| 2 | `-- Isolate client notes & files per user (admins see all)` | Comentario da migration; explica a intencao daquele trecho SQL. |
| 3 | `(linha em branco)` | Separa blocos de codigo para melhorar a leitura. |
| 4 | `-- client_notes: created_by owns` | Comentario da migration; explica a intencao daquele trecho SQL. |
| 5 | `DROP POLICY IF EXISTS client_notes_all_auth ON public.client_notes;` | Remove objeto antigo para permitir recriacao ou limpeza controlada. |
| 6 | `CREATE POLICY client_notes_select ON public.client_notes FOR SELECT TO authenticated` | Cria uma regra de seguranca RLS para controlar acesso aos dados. |
| 7 | `  USING (public.has_role(auth.uid(),'admin') OR created_by = auth.uid());` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 8 | `CREATE POLICY client_notes_insert ON public.client_notes FOR INSERT TO authenticated` | Cria uma regra de seguranca RLS para controlar acesso aos dados. |
| 9 | `  WITH CHECK (created_by = auth.uid() OR created_by IS NULL);` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 10 | `CREATE POLICY client_notes_update ON public.client_notes FOR UPDATE TO authenticated` | Cria uma regra de seguranca RLS para controlar acesso aos dados. |
| 11 | `  USING (public.has_role(auth.uid(),'admin') OR created_by = auth.uid())` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 12 | `  WITH CHECK (public.has_role(auth.uid(),'admin') OR created_by = auth.uid());` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 13 | `CREATE POLICY client_notes_delete ON public.client_notes FOR DELETE TO authenticated` | Cria uma regra de seguranca RLS para controlar acesso aos dados. |
| 14 | `  USING (public.has_role(auth.uid(),'admin') OR created_by = auth.uid());` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 15 | `(linha em branco)` | Separa blocos de codigo para melhorar a leitura. |
| 16 | `-- auto-fill created_by on insert` | Comentario da migration; explica a intencao daquele trecho SQL. |
| 17 | `CREATE OR REPLACE FUNCTION public.set_client_note_created_by()` | Define uma funcao no banco para reutilizar logica SQL. |
| 18 | `RETURNS trigger LANGUAGE plpgsql SET search_path=public AS $$` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 19 | `BEGIN` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 20 | `  IF NEW.created_by IS NULL THEN NEW.created_by := auth.uid(); END IF;` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 21 | `  RETURN NEW;` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 22 | `END $$;` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 23 | `DROP TRIGGER IF EXISTS trg_client_notes_created_by ON public.client_notes;` | Remove objeto antigo para permitir recriacao ou limpeza controlada. |
| 24 | `CREATE TRIGGER trg_client_notes_created_by BEFORE INSERT ON public.client_notes` | Cria gatilho que executa uma funcao automaticamente em eventos da tabela. |
| 25 | `  FOR EACH ROW EXECUTE FUNCTION public.set_client_note_created_by();` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 26 | `(linha em branco)` | Separa blocos de codigo para melhorar a leitura. |
| 27 | `-- client_files: uploaded_by owns` | Comentario da migration; explica a intencao daquele trecho SQL. |
| 28 | `DROP POLICY IF EXISTS "Authenticated can manage client_files" ON public.client_files;` | Remove objeto antigo para permitir recriacao ou limpeza controlada. |
| 29 | `CREATE POLICY client_files_select ON public.client_files FOR SELECT TO authenticated` | Cria uma regra de seguranca RLS para controlar acesso aos dados. |
| 30 | `  USING (public.has_role(auth.uid(),'admin') OR uploaded_by = auth.uid());` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 31 | `CREATE POLICY client_files_insert ON public.client_files FOR INSERT TO authenticated` | Cria uma regra de seguranca RLS para controlar acesso aos dados. |
| 32 | `  WITH CHECK (uploaded_by = auth.uid() OR uploaded_by IS NULL);` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 33 | `CREATE POLICY client_files_update ON public.client_files FOR UPDATE TO authenticated` | Cria uma regra de seguranca RLS para controlar acesso aos dados. |
| 34 | `  USING (public.has_role(auth.uid(),'admin') OR uploaded_by = auth.uid())` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 35 | `  WITH CHECK (public.has_role(auth.uid(),'admin') OR uploaded_by = auth.uid());` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 36 | `CREATE POLICY client_files_delete ON public.client_files FOR DELETE TO authenticated` | Cria uma regra de seguranca RLS para controlar acesso aos dados. |
| 37 | `  USING (public.has_role(auth.uid(),'admin') OR uploaded_by = auth.uid());` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 38 | `(linha em branco)` | Separa blocos de codigo para melhorar a leitura. |
| 39 | `CREATE OR REPLACE FUNCTION public.set_client_file_uploaded_by()` | Define uma funcao no banco para reutilizar logica SQL. |
| 40 | `RETURNS trigger LANGUAGE plpgsql SET search_path=public AS $$` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 41 | `BEGIN` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 42 | `  IF NEW.uploaded_by IS NULL THEN NEW.uploaded_by := auth.uid(); END IF;` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 43 | `  RETURN NEW;` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 44 | `END $$;` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 45 | `DROP TRIGGER IF EXISTS trg_client_files_uploaded_by ON public.client_files;` | Remove objeto antigo para permitir recriacao ou limpeza controlada. |
| 46 | `CREATE TRIGGER trg_client_files_uploaded_by BEFORE INSERT ON public.client_files` | Cria gatilho que executa uma funcao automaticamente em eventos da tabela. |
| 47 | `  FOR EACH ROW EXECUTE FUNCTION public.set_client_file_uploaded_by();` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 48 | `(linha em branco)` | Separa blocos de codigo para melhorar a leitura. |
| 49 | `-- client_note_attachments: visibility follows parent note` | Comentario da migration; explica a intencao daquele trecho SQL. |
| 50 | `DROP POLICY IF EXISTS client_note_att_all_auth ON public.client_note_attachments;` | Remove objeto antigo para permitir recriacao ou limpeza controlada. |
| 51 | `CREATE POLICY client_note_att_select ON public.client_note_attachments FOR SELECT TO authenticated` | Cria uma regra de seguranca RLS para controlar acesso aos dados. |
| 52 | `  USING (EXISTS (SELECT 1 FROM public.client_notes n WHERE n.id = note_id` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 53 | `    AND (public.has_role(auth.uid(),'admin') OR n.created_by = auth.uid())));` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 54 | `CREATE POLICY client_note_att_insert ON public.client_note_attachments FOR INSERT TO authenticated` | Cria uma regra de seguranca RLS para controlar acesso aos dados. |
| 55 | `  WITH CHECK (EXISTS (SELECT 1 FROM public.client_notes n WHERE n.id = note_id` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 56 | `    AND (public.has_role(auth.uid(),'admin') OR n.created_by = auth.uid())));` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 57 | `CREATE POLICY client_note_att_update ON public.client_note_attachments FOR UPDATE TO authenticated` | Cria uma regra de seguranca RLS para controlar acesso aos dados. |
| 58 | `  USING (EXISTS (SELECT 1 FROM public.client_notes n WHERE n.id = note_id` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 59 | `    AND (public.has_role(auth.uid(),'admin') OR n.created_by = auth.uid())));` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 60 | `CREATE POLICY client_note_att_delete ON public.client_note_attachments FOR DELETE TO authenticated` | Cria uma regra de seguranca RLS para controlar acesso aos dados. |
| 61 | `  USING (EXISTS (SELECT 1 FROM public.client_notes n WHERE n.id = note_id` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 62 | `    AND (public.has_role(auth.uid(),'admin') OR n.created_by = auth.uid())));` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 63 | `(linha em branco)` | Separa blocos de codigo para melhorar a leitura. |
