# supabase/migrations/20260611110453_13b957de-b9dc-44a9-8356-416f4cacd27a.sql

Tipo: Migration SQL do Supabase.

Este documento foi gerado para estudo e manutencao. Ele preserva o codigo original e explica linha por linha sem alterar o comportamento do sistema publicado.

| Linha | Codigo original | Explicacao |
| ---: | --- | --- |
| 1 | `(linha em branco)` | Separa blocos de codigo para melhorar a leitura. |
| 2 | `CREATE TABLE public.client_files (` | Cria uma tabela no banco de dados Supabase/PostgreSQL. |
| 3 | `  id UUID NOT NULL DEFAULT gen_random_uuid() PRIMARY KEY,` | Define identificador unico principal do registro. |
| 4 | `  client_id UUID NOT NULL REFERENCES public.clients(id) ON DELETE CASCADE,` | Define relacionamento entre tabelas por chave estrangeira. |
| 5 | `  file_name TEXT NOT NULL,` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 6 | `  storage_path TEXT NOT NULL,` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 7 | `  mime_type TEXT,` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 8 | `  size_bytes BIGINT,` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 9 | `  uploaded_by UUID REFERENCES auth.users(id) ON DELETE SET NULL,` | Define relacionamento entre tabelas por chave estrangeira. |
| 10 | `  created_at TIMESTAMPTZ NOT NULL DEFAULT now()` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 11 | `);` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 12 | `(linha em branco)` | Separa blocos de codigo para melhorar a leitura. |
| 13 | `CREATE INDEX client_files_client_id_idx ON public.client_files(client_id);` | Cria indice para acelerar consultas frequentes. |
| 14 | `(linha em branco)` | Separa blocos de codigo para melhorar a leitura. |
| 15 | `GRANT SELECT, INSERT, UPDATE, DELETE ON public.client_files TO authenticated;` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 16 | `GRANT ALL ON public.client_files TO service_role;` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 17 | `(linha em branco)` | Separa blocos de codigo para melhorar a leitura. |
| 18 | `ALTER TABLE public.client_files ENABLE ROW LEVEL SECURITY;` | Altera uma tabela existente, normalmente adicionando campos, chaves ou politicas. |
| 19 | `(linha em branco)` | Separa blocos de codigo para melhorar a leitura. |
| 20 | `CREATE POLICY "Authenticated can manage client_files"` | Cria uma regra de seguranca RLS para controlar acesso aos dados. |
| 21 | `ON public.client_files FOR ALL` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 22 | `TO authenticated` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 23 | `USING (true) WITH CHECK (true);` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 24 | `(linha em branco)` | Separa blocos de codigo para melhorar a leitura. |
