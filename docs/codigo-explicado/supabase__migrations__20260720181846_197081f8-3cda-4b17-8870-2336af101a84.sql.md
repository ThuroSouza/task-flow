# supabase/migrations/20260720181846_197081f8-3cda-4b17-8870-2336af101a84.sql

Tipo: Migration SQL do Supabase.

Este documento foi gerado para estudo e manutencao. Ele preserva o codigo original e explica linha por linha sem alterar o comportamento do sistema publicado.

| Linha | Codigo original | Explicacao |
| ---: | --- | --- |
| 1 | `(linha em branco)` | Separa blocos de codigo para melhorar a leitura. |
| 2 | `CREATE TABLE IF NOT EXISTS public.notifications (` | Cria uma tabela no banco de dados Supabase/PostgreSQL. |
| 3 | `  id uuid PRIMARY KEY DEFAULT gen_random_uuid(),` | Define identificador unico principal do registro. |
| 4 | `  user_id uuid NOT NULL,` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 5 | `  task_id uuid REFERENCES public.tasks(id) ON DELETE CASCADE,` | Define relacionamento entre tabelas por chave estrangeira. |
| 6 | `  type text NOT NULL,` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 7 | `  title text NOT NULL,` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 8 | `  body text,` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 9 | `  is_read boolean NOT NULL DEFAULT false,` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 10 | `  created_at timestamptz NOT NULL DEFAULT now()` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 11 | `);` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 12 | `(linha em branco)` | Separa blocos de codigo para melhorar a leitura. |
| 13 | `CREATE INDEX IF NOT EXISTS notifications_user_created_idx` | Cria indice para acelerar consultas frequentes. |
| 14 | `  ON public.notifications (user_id, created_at DESC);` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 15 | `(linha em branco)` | Separa blocos de codigo para melhorar a leitura. |
| 16 | `GRANT SELECT, INSERT, UPDATE, DELETE ON public.notifications TO authenticated;` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 17 | `GRANT ALL ON public.notifications TO service_role;` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 18 | `(linha em branco)` | Separa blocos de codigo para melhorar a leitura. |
| 19 | `ALTER TABLE public.notifications ENABLE ROW LEVEL SECURITY;` | Altera uma tabela existente, normalmente adicionando campos, chaves ou politicas. |
| 20 | `(linha em branco)` | Separa blocos de codigo para melhorar a leitura. |
| 21 | `DROP POLICY IF EXISTS "own_notifs_select" ON public.notifications;` | Remove objeto antigo para permitir recriacao ou limpeza controlada. |
| 22 | `CREATE POLICY "own_notifs_select" ON public.notifications` | Cria uma regra de seguranca RLS para controlar acesso aos dados. |
| 23 | `  FOR SELECT TO authenticated USING (auth.uid() = user_id);` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 24 | `(linha em branco)` | Separa blocos de codigo para melhorar a leitura. |
| 25 | `DROP POLICY IF EXISTS "own_notifs_update" ON public.notifications;` | Remove objeto antigo para permitir recriacao ou limpeza controlada. |
| 26 | `CREATE POLICY "own_notifs_update" ON public.notifications` | Cria uma regra de seguranca RLS para controlar acesso aos dados. |
| 27 | `  FOR UPDATE TO authenticated USING (auth.uid() = user_id) WITH CHECK (auth.uid() = user_id);` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 28 | `(linha em branco)` | Separa blocos de codigo para melhorar a leitura. |
| 29 | `DROP POLICY IF EXISTS "own_notifs_delete" ON public.notifications;` | Remove objeto antigo para permitir recriacao ou limpeza controlada. |
| 30 | `CREATE POLICY "own_notifs_delete" ON public.notifications` | Cria uma regra de seguranca RLS para controlar acesso aos dados. |
| 31 | `  FOR DELETE TO authenticated USING (auth.uid() = user_id);` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 32 | `(linha em branco)` | Separa blocos de codigo para melhorar a leitura. |
| 33 | `-- allow system triggers (SECURITY DEFINER functions) to insert; also allow` | Comentario da migration; explica a intencao daquele trecho SQL. |
| 34 | `-- authenticated inserts scoped to any user because triggers run as the caller` | Comentario da migration; explica a intencao daquele trecho SQL. |
| 35 | `DROP POLICY IF EXISTS "notifs_insert_any" ON public.notifications;` | Remove objeto antigo para permitir recriacao ou limpeza controlada. |
| 36 | `CREATE POLICY "notifs_insert_any" ON public.notifications` | Cria uma regra de seguranca RLS para controlar acesso aos dados. |
| 37 | `  FOR INSERT TO authenticated WITH CHECK (true);` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 38 | `(linha em branco)` | Separa blocos de codigo para melhorar a leitura. |
| 39 | `-- Trigger: notify assignee when a task is assigned (or reassigned) to someone` | Comentario da migration; explica a intencao daquele trecho SQL. |
| 40 | `CREATE OR REPLACE FUNCTION public.notify_task_assignment()` | Define uma funcao no banco para reutilizar logica SQL. |
| 41 | `RETURNS trigger` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 42 | `LANGUAGE plpgsql` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 43 | `SECURITY DEFINER` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 44 | `SET search_path = public` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 45 | `AS $$` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 46 | `DECLARE` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 47 | `  actor uuid := auth.uid();` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 48 | `  assigner_name text;` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 49 | `BEGIN` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 50 | `  IF NEW.assignee_id IS NULL THEN RETURN NEW; END IF;` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 51 | `  IF TG_OP = 'UPDATE' AND OLD.assignee_id IS NOT DISTINCT FROM NEW.assignee_id THEN` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 52 | `    RETURN NEW;` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 53 | `  END IF;` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 54 | `  IF NEW.assignee_id = actor THEN RETURN NEW; END IF;` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 55 | `(linha em branco)` | Separa blocos de codigo para melhorar a leitura. |
| 56 | `  SELECT COALESCE(full_name, email) INTO assigner_name FROM public.profiles WHERE id = actor;` | Consulta dados ou valida uma condicao no banco. |
| 57 | `(linha em branco)` | Separa blocos de codigo para melhorar a leitura. |
| 58 | `  INSERT INTO public.notifications (user_id, task_id, type, title, body)` | Insere dados iniciais ou registros de apoio. |
| 59 | `  VALUES (` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 60 | `    NEW.assignee_id,` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 61 | `    NEW.id,` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 62 | `    'assignment',` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 63 | `    'Nova tarefa atribuída a você',` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 64 | `    COALESCE(assigner_name, 'Alguém') || ' atribuiu: ' || NEW.title` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 65 | `  );` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 66 | `  RETURN NEW;` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 67 | `END;` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 68 | `$$;` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 69 | `(linha em branco)` | Separa blocos de codigo para melhorar a leitura. |
| 70 | `DROP TRIGGER IF EXISTS trg_notify_task_assignment ON public.tasks;` | Remove objeto antigo para permitir recriacao ou limpeza controlada. |
| 71 | `CREATE TRIGGER trg_notify_task_assignment` | Cria gatilho que executa uma funcao automaticamente em eventos da tabela. |
| 72 | `AFTER INSERT OR UPDATE OF assignee_id ON public.tasks` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 73 | `FOR EACH ROW EXECUTE FUNCTION public.notify_task_assignment();` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 74 | `(linha em branco)` | Separa blocos de codigo para melhorar a leitura. |
| 75 | `-- Trigger: notify assignee on new comment (if author != assignee)` | Comentario da migration; explica a intencao daquele trecho SQL. |
| 76 | `CREATE OR REPLACE FUNCTION public.notify_task_comment()` | Define uma funcao no banco para reutilizar logica SQL. |
| 77 | `RETURNS trigger` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 78 | `LANGUAGE plpgsql` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 79 | `SECURITY DEFINER` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 80 | `SET search_path = public` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 81 | `AS $$` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 82 | `DECLARE` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 83 | `  t record;` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 84 | `  author_name text;` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 85 | `BEGIN` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 86 | `  SELECT id, title, assignee_id INTO t FROM public.tasks WHERE id = NEW.task_id;` | Consulta dados ou valida uma condicao no banco. |
| 87 | `  IF t.assignee_id IS NULL OR t.assignee_id = NEW.created_by THEN RETURN NEW; END IF;` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 88 | `(linha em branco)` | Separa blocos de codigo para melhorar a leitura. |
| 89 | `  SELECT COALESCE(full_name, email) INTO author_name FROM public.profiles WHERE id = NEW.created_by;` | Consulta dados ou valida uma condicao no banco. |
| 90 | `(linha em branco)` | Separa blocos de codigo para melhorar a leitura. |
| 91 | `  INSERT INTO public.notifications (user_id, task_id, type, title, body)` | Insere dados iniciais ou registros de apoio. |
| 92 | `  VALUES (` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 93 | `    t.assignee_id,` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 94 | `    t.id,` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 95 | `    'comment',` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 96 | `    'Nova observação em sua tarefa',` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 97 | `    COALESCE(author_name, 'Alguém') || ' comentou em: ' || t.title` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 98 | `  );` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 99 | `  RETURN NEW;` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 100 | `END;` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 101 | `$$;` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 102 | `(linha em branco)` | Separa blocos de codigo para melhorar a leitura. |
| 103 | `DROP TRIGGER IF EXISTS trg_notify_task_comment ON public.comments;` | Remove objeto antigo para permitir recriacao ou limpeza controlada. |
| 104 | `CREATE TRIGGER trg_notify_task_comment` | Cria gatilho que executa uma funcao automaticamente em eventos da tabela. |
| 105 | `AFTER INSERT ON public.comments` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 106 | `FOR EACH ROW EXECUTE FUNCTION public.notify_task_comment();` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 107 | `(linha em branco)` | Separa blocos de codigo para melhorar a leitura. |
| 108 | `-- Trigger: notify assignee on new attachment (if uploader != assignee)` | Comentario da migration; explica a intencao daquele trecho SQL. |
| 109 | `CREATE OR REPLACE FUNCTION public.notify_task_attachment()` | Define uma funcao no banco para reutilizar logica SQL. |
| 110 | `RETURNS trigger` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 111 | `LANGUAGE plpgsql` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 112 | `SECURITY DEFINER` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 113 | `SET search_path = public` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 114 | `AS $$` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 115 | `DECLARE` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 116 | `  t record;` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 117 | `  uploader_name text;` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 118 | `BEGIN` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 119 | `  SELECT id, title, assignee_id INTO t FROM public.tasks WHERE id = NEW.task_id;` | Consulta dados ou valida uma condicao no banco. |
| 120 | `  IF t.assignee_id IS NULL OR t.assignee_id = NEW.uploaded_by THEN RETURN NEW; END IF;` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 121 | `(linha em branco)` | Separa blocos de codigo para melhorar a leitura. |
| 122 | `  SELECT COALESCE(full_name, email) INTO uploader_name FROM public.profiles WHERE id = NEW.uploaded_by;` | Consulta dados ou valida uma condicao no banco. |
| 123 | `(linha em branco)` | Separa blocos de codigo para melhorar a leitura. |
| 124 | `  INSERT INTO public.notifications (user_id, task_id, type, title, body)` | Insere dados iniciais ou registros de apoio. |
| 125 | `  VALUES (` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 126 | `    t.assignee_id,` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 127 | `    t.id,` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 128 | `    'attachment',` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 129 | `    'Novo arquivo em sua tarefa',` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 130 | `    COALESCE(uploader_name, 'Alguém') || ' anexou "' || NEW.file_name || '" em: ' || t.title` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 131 | `  );` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 132 | `  RETURN NEW;` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 133 | `END;` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 134 | `$$;` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 135 | `(linha em branco)` | Separa blocos de codigo para melhorar a leitura. |
| 136 | `DROP TRIGGER IF EXISTS trg_notify_task_attachment ON public.attachments;` | Remove objeto antigo para permitir recriacao ou limpeza controlada. |
| 137 | `CREATE TRIGGER trg_notify_task_attachment` | Cria gatilho que executa uma funcao automaticamente em eventos da tabela. |
| 138 | `AFTER INSERT ON public.attachments` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 139 | `FOR EACH ROW EXECUTE FUNCTION public.notify_task_attachment();` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 140 | `(linha em branco)` | Separa blocos de codigo para melhorar a leitura. |
| 141 | `ALTER PUBLICATION supabase_realtime ADD TABLE public.notifications;` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 142 | `(linha em branco)` | Separa blocos de codigo para melhorar a leitura. |
