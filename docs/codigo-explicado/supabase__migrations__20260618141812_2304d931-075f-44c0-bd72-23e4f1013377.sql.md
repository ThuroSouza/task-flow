# supabase/migrations/20260618141812_2304d931-075f-44c0-bd72-23e4f1013377.sql

Tipo: Migration SQL do Supabase.

Este documento foi gerado para estudo e manutencao. Ele preserva o codigo original e explica linha por linha sem alterar o comportamento do sistema publicado.

| Linha | Codigo original | Explicacao |
| ---: | --- | --- |
| 1 | `(linha em branco)` | Separa blocos de codigo para melhorar a leitura. |
| 2 | `UPDATE public.task_statuses` | Atualiza registros existentes no banco. |
| 3 | `SET is_completed = true` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 4 | `WHERE is_completed = false` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 5 | `  AND lower(name) IN ('concluída','concluida','concluído','concluido','done','completed','finalizada','finalizado');` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 6 | `(linha em branco)` | Separa blocos de codigo para melhorar a leitura. |
| 7 | `UPDATE public.tasks` | Atualiza registros existentes no banco. |
| 8 | `SET completed_at = COALESCE(completed_at, updated_at, now())` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 9 | `WHERE deleted_at IS NULL` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 10 | `  AND status = 'done'` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 11 | `  AND completed_at IS NULL;` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 12 | `(linha em branco)` | Separa blocos de codigo para melhorar a leitura. |
| 13 | `UPDATE public.tasks t` | Atualiza registros existentes no banco. |
| 14 | `SET status_id = ts.id` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 15 | `FROM public.task_statuses ts` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 16 | `WHERE t.deleted_at IS NULL` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 17 | `  AND t.status = 'done'` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 18 | `  AND ts.is_completed = true` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 19 | `  AND ts.created_by = t.created_by` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 20 | `  AND (` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 21 | `    t.status_id IS NULL` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 22 | `    OR NOT EXISTS (` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 23 | `      SELECT 1 FROM public.task_statuses s2` | Consulta dados ou valida uma condicao no banco. |
| 24 | `      WHERE s2.id = t.status_id AND s2.is_completed = true` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 25 | `    )` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 26 | `  );` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 27 | `(linha em branco)` | Separa blocos de codigo para melhorar a leitura. |
| 28 | `UPDATE public.tasks t` | Atualiza registros existentes no banco. |
| 29 | `SET status = 'done',` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 30 | `    completed_at = COALESCE(t.completed_at, t.updated_at, now())` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 31 | `FROM public.task_statuses ts` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 32 | `WHERE t.deleted_at IS NULL` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 33 | `  AND t.status_id = ts.id` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 34 | `  AND ts.is_completed = true` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 35 | `  AND (t.status <> 'done' OR t.completed_at IS NULL);` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 36 | `(linha em branco)` | Separa blocos de codigo para melhorar a leitura. |
