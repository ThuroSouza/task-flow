# supabase/migrations/20260604160628_6e7127a9-d36d-4297-9cb8-4e27c8ac180f.sql

Tipo: Migration SQL do Supabase.

Este documento foi gerado para estudo e manutencao. Ele preserva o codigo original e explica linha por linha sem alterar o comportamento do sistema publicado.

| Linha | Codigo original | Explicacao |
| ---: | --- | --- |
| 1 | `(linha em branco)` | Separa blocos de codigo para melhorar a leitura. |
| 2 | `-- ============ ENUMS ============` | Comentario da migration; explica a intencao daquele trecho SQL. |
| 3 | `CREATE TYPE public.app_role AS ENUM ('admin', 'member');` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 4 | `CREATE TYPE public.task_status AS ENUM ('todo', 'in_progress', 'review', 'done');` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 5 | `CREATE TYPE public.task_priority AS ENUM ('low', 'medium', 'high', 'urgent');` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 6 | `(linha em branco)` | Separa blocos de codigo para melhorar a leitura. |
| 7 | `-- ============ PROFILES ============` | Comentario da migration; explica a intencao daquele trecho SQL. |
| 8 | `CREATE TABLE public.profiles (` | Cria uma tabela no banco de dados Supabase/PostgreSQL. |
| 9 | `  id UUID PRIMARY KEY REFERENCES auth.users(id) ON DELETE CASCADE,` | Define relacionamento entre tabelas por chave estrangeira. |
| 10 | `  full_name TEXT,` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 11 | `  email TEXT,` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 12 | `  avatar_url TEXT,` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 13 | `  color TEXT DEFAULT '#1e3a8a',` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 14 | `  theme_preferences JSONB DEFAULT '{}'::jsonb,` | Define relacionamento entre tabelas por chave estrangeira. |
| 15 | `  created_at TIMESTAMPTZ NOT NULL DEFAULT now(),` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 16 | `  updated_at TIMESTAMPTZ NOT NULL DEFAULT now()` | Atualiza registros existentes no banco. |
| 17 | `);` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 18 | `GRANT SELECT, INSERT, UPDATE, DELETE ON public.profiles TO authenticated;` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 19 | `GRANT ALL ON public.profiles TO service_role;` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 20 | `ALTER TABLE public.profiles ENABLE ROW LEVEL SECURITY;` | Altera uma tabela existente, normalmente adicionando campos, chaves ou politicas. |
| 21 | `(linha em branco)` | Separa blocos de codigo para melhorar a leitura. |
| 22 | `CREATE POLICY "profiles_select_all_auth" ON public.profiles FOR SELECT TO authenticated USING (true);` | Cria uma regra de seguranca RLS para controlar acesso aos dados. |
| 23 | `CREATE POLICY "profiles_update_own" ON public.profiles FOR UPDATE TO authenticated USING (auth.uid() = id) WITH CHECK (auth.uid() = id);` | Cria uma regra de seguranca RLS para controlar acesso aos dados. |
| 24 | `CREATE POLICY "profiles_insert_own" ON public.profiles FOR INSERT TO authenticated WITH CHECK (auth.uid() = id);` | Cria uma regra de seguranca RLS para controlar acesso aos dados. |
| 25 | `(linha em branco)` | Separa blocos de codigo para melhorar a leitura. |
| 26 | `-- ============ USER ROLES ============` | Comentario da migration; explica a intencao daquele trecho SQL. |
| 27 | `CREATE TABLE public.user_roles (` | Cria uma tabela no banco de dados Supabase/PostgreSQL. |
| 28 | `  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),` | Define identificador unico principal do registro. |
| 29 | `  user_id UUID NOT NULL REFERENCES auth.users(id) ON DELETE CASCADE,` | Define relacionamento entre tabelas por chave estrangeira. |
| 30 | `  role app_role NOT NULL DEFAULT 'member',` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 31 | `  created_at TIMESTAMPTZ NOT NULL DEFAULT now(),` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 32 | `  UNIQUE (user_id, role)` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 33 | `);` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 34 | `GRANT SELECT ON public.user_roles TO authenticated;` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 35 | `GRANT ALL ON public.user_roles TO service_role;` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 36 | `ALTER TABLE public.user_roles ENABLE ROW LEVEL SECURITY;` | Altera uma tabela existente, normalmente adicionando campos, chaves ou politicas. |
| 37 | `(linha em branco)` | Separa blocos de codigo para melhorar a leitura. |
| 38 | `CREATE OR REPLACE FUNCTION public.has_role(_user_id UUID, _role app_role)` | Define uma funcao no banco para reutilizar logica SQL. |
| 39 | `RETURNS BOOLEAN` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 40 | `LANGUAGE SQL STABLE SECURITY DEFINER SET search_path = public` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 41 | `AS $$` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 42 | `  SELECT EXISTS (SELECT 1 FROM public.user_roles WHERE user_id = _user_id AND role = _role)` | Consulta dados ou valida uma condicao no banco. |
| 43 | `$$;` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 44 | `(linha em branco)` | Separa blocos de codigo para melhorar a leitura. |
| 45 | `CREATE POLICY "roles_select_all_auth" ON public.user_roles FOR SELECT TO authenticated USING (true);` | Cria uma regra de seguranca RLS para controlar acesso aos dados. |
| 46 | `CREATE POLICY "roles_admin_manage" ON public.user_roles FOR ALL TO authenticated` | Cria uma regra de seguranca RLS para controlar acesso aos dados. |
| 47 | `  USING (public.has_role(auth.uid(), 'admin'))` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 48 | `  WITH CHECK (public.has_role(auth.uid(), 'admin'));` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 49 | `(linha em branco)` | Separa blocos de codigo para melhorar a leitura. |
| 50 | `-- ============ CLIENTS ============` | Comentario da migration; explica a intencao daquele trecho SQL. |
| 51 | `CREATE TABLE public.clients (` | Cria uma tabela no banco de dados Supabase/PostgreSQL. |
| 52 | `  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),` | Define identificador unico principal do registro. |
| 53 | `  name TEXT NOT NULL,` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 54 | `  color TEXT DEFAULT '#1e3a8a',` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 55 | `  description TEXT,` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 56 | `  created_by UUID REFERENCES auth.users(id) ON DELETE SET NULL,` | Define relacionamento entre tabelas por chave estrangeira. |
| 57 | `  created_at TIMESTAMPTZ NOT NULL DEFAULT now(),` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 58 | `  updated_at TIMESTAMPTZ NOT NULL DEFAULT now()` | Atualiza registros existentes no banco. |
| 59 | `);` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 60 | `GRANT SELECT, INSERT, UPDATE, DELETE ON public.clients TO authenticated;` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 61 | `GRANT ALL ON public.clients TO service_role;` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 62 | `ALTER TABLE public.clients ENABLE ROW LEVEL SECURITY;` | Altera uma tabela existente, normalmente adicionando campos, chaves ou politicas. |
| 63 | `CREATE POLICY "clients_all_auth" ON public.clients FOR ALL TO authenticated USING (true) WITH CHECK (true);` | Cria uma regra de seguranca RLS para controlar acesso aos dados. |
| 64 | `(linha em branco)` | Separa blocos de codigo para melhorar a leitura. |
| 65 | `-- ============ KANBAN COLUMNS ============` | Comentario da migration; explica a intencao daquele trecho SQL. |
| 66 | `CREATE TABLE public.kanban_columns (` | Cria uma tabela no banco de dados Supabase/PostgreSQL. |
| 67 | `  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),` | Define identificador unico principal do registro. |
| 68 | `  name TEXT NOT NULL,` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 69 | `  color TEXT DEFAULT '#1e3a8a',` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 70 | `  position INT NOT NULL DEFAULT 0,` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 71 | `  client_id UUID REFERENCES public.clients(id) ON DELETE SET NULL,` | Define relacionamento entre tabelas por chave estrangeira. |
| 72 | `  created_by UUID REFERENCES auth.users(id) ON DELETE SET NULL,` | Define relacionamento entre tabelas por chave estrangeira. |
| 73 | `  created_at TIMESTAMPTZ NOT NULL DEFAULT now(),` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 74 | `  updated_at TIMESTAMPTZ NOT NULL DEFAULT now()` | Atualiza registros existentes no banco. |
| 75 | `);` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 76 | `GRANT SELECT, INSERT, UPDATE, DELETE ON public.kanban_columns TO authenticated;` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 77 | `GRANT ALL ON public.kanban_columns TO service_role;` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 78 | `ALTER TABLE public.kanban_columns ENABLE ROW LEVEL SECURITY;` | Altera uma tabela existente, normalmente adicionando campos, chaves ou politicas. |
| 79 | `CREATE POLICY "cols_all_auth" ON public.kanban_columns FOR ALL TO authenticated USING (true) WITH CHECK (true);` | Cria uma regra de seguranca RLS para controlar acesso aos dados. |
| 80 | `(linha em branco)` | Separa blocos de codigo para melhorar a leitura. |
| 81 | `-- ============ TASKS ============` | Comentario da migration; explica a intencao daquele trecho SQL. |
| 82 | `CREATE TABLE public.tasks (` | Cria uma tabela no banco de dados Supabase/PostgreSQL. |
| 83 | `  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),` | Define identificador unico principal do registro. |
| 84 | `  title TEXT NOT NULL,` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 85 | `  description TEXT,` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 86 | `  status task_status NOT NULL DEFAULT 'todo',` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 87 | `  priority task_priority NOT NULL DEFAULT 'medium',` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 88 | `  due_date TIMESTAMPTZ,` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 89 | `  assignee_id UUID REFERENCES auth.users(id) ON DELETE SET NULL,` | Define relacionamento entre tabelas por chave estrangeira. |
| 90 | `  client_id UUID REFERENCES public.clients(id) ON DELETE SET NULL,` | Define relacionamento entre tabelas por chave estrangeira. |
| 91 | `  column_id UUID REFERENCES public.kanban_columns(id) ON DELETE SET NULL,` | Define relacionamento entre tabelas por chave estrangeira. |
| 92 | `  position INT NOT NULL DEFAULT 0,` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 93 | `  color TEXT,` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 94 | `  completed_at TIMESTAMPTZ,` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 95 | `  created_by UUID REFERENCES auth.users(id) ON DELETE SET NULL,` | Define relacionamento entre tabelas por chave estrangeira. |
| 96 | `  created_at TIMESTAMPTZ NOT NULL DEFAULT now(),` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 97 | `  updated_at TIMESTAMPTZ NOT NULL DEFAULT now()` | Atualiza registros existentes no banco. |
| 98 | `);` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 99 | `GRANT SELECT, INSERT, UPDATE, DELETE ON public.tasks TO authenticated;` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 100 | `GRANT ALL ON public.tasks TO service_role;` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 101 | `ALTER TABLE public.tasks ENABLE ROW LEVEL SECURITY;` | Altera uma tabela existente, normalmente adicionando campos, chaves ou politicas. |
| 102 | `CREATE POLICY "tasks_all_auth" ON public.tasks FOR ALL TO authenticated USING (true) WITH CHECK (true);` | Cria uma regra de seguranca RLS para controlar acesso aos dados. |
| 103 | `CREATE INDEX tasks_column_idx ON public.tasks(column_id);` | Cria indice para acelerar consultas frequentes. |
| 104 | `CREATE INDEX tasks_assignee_idx ON public.tasks(assignee_id);` | Cria indice para acelerar consultas frequentes. |
| 105 | `CREATE INDEX tasks_due_idx ON public.tasks(due_date);` | Cria indice para acelerar consultas frequentes. |
| 106 | `(linha em branco)` | Separa blocos de codigo para melhorar a leitura. |
| 107 | `-- ============ SUBTASKS ============` | Comentario da migration; explica a intencao daquele trecho SQL. |
| 108 | `CREATE TABLE public.subtasks (` | Cria uma tabela no banco de dados Supabase/PostgreSQL. |
| 109 | `  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),` | Define identificador unico principal do registro. |
| 110 | `  task_id UUID NOT NULL REFERENCES public.tasks(id) ON DELETE CASCADE,` | Define relacionamento entre tabelas por chave estrangeira. |
| 111 | `  title TEXT NOT NULL,` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 112 | `  done BOOLEAN NOT NULL DEFAULT false,` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 113 | `  position INT NOT NULL DEFAULT 0,` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 114 | `  created_at TIMESTAMPTZ NOT NULL DEFAULT now()` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 115 | `);` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 116 | `GRANT SELECT, INSERT, UPDATE, DELETE ON public.subtasks TO authenticated;` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 117 | `GRANT ALL ON public.subtasks TO service_role;` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 118 | `ALTER TABLE public.subtasks ENABLE ROW LEVEL SECURITY;` | Altera uma tabela existente, normalmente adicionando campos, chaves ou politicas. |
| 119 | `CREATE POLICY "subtasks_all_auth" ON public.subtasks FOR ALL TO authenticated USING (true) WITH CHECK (true);` | Cria uma regra de seguranca RLS para controlar acesso aos dados. |
| 120 | `(linha em branco)` | Separa blocos de codigo para melhorar a leitura. |
| 121 | `-- ============ COMMENTS ============` | Comentario da migration; explica a intencao daquele trecho SQL. |
| 122 | `CREATE TABLE public.comments (` | Cria uma tabela no banco de dados Supabase/PostgreSQL. |
| 123 | `  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),` | Define identificador unico principal do registro. |
| 124 | `  task_id UUID NOT NULL REFERENCES public.tasks(id) ON DELETE CASCADE,` | Define relacionamento entre tabelas por chave estrangeira. |
| 125 | `  author_id UUID NOT NULL REFERENCES auth.users(id) ON DELETE CASCADE,` | Define relacionamento entre tabelas por chave estrangeira. |
| 126 | `  body TEXT NOT NULL,` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 127 | `  created_at TIMESTAMPTZ NOT NULL DEFAULT now()` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 128 | `);` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 129 | `GRANT SELECT, INSERT, UPDATE, DELETE ON public.comments TO authenticated;` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 130 | `GRANT ALL ON public.comments TO service_role;` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 131 | `ALTER TABLE public.comments ENABLE ROW LEVEL SECURITY;` | Altera uma tabela existente, normalmente adicionando campos, chaves ou politicas. |
| 132 | `CREATE POLICY "comments_select_all" ON public.comments FOR SELECT TO authenticated USING (true);` | Cria uma regra de seguranca RLS para controlar acesso aos dados. |
| 133 | `CREATE POLICY "comments_insert_self" ON public.comments FOR INSERT TO authenticated WITH CHECK (auth.uid() = author_id);` | Cria uma regra de seguranca RLS para controlar acesso aos dados. |
| 134 | `CREATE POLICY "comments_modify_own" ON public.comments FOR UPDATE TO authenticated USING (auth.uid() = author_id);` | Cria uma regra de seguranca RLS para controlar acesso aos dados. |
| 135 | `CREATE POLICY "comments_delete_own" ON public.comments FOR DELETE TO authenticated USING (auth.uid() = author_id);` | Cria uma regra de seguranca RLS para controlar acesso aos dados. |
| 136 | `(linha em branco)` | Separa blocos de codigo para melhorar a leitura. |
| 137 | `-- ============ ATTACHMENTS ============` | Comentario da migration; explica a intencao daquele trecho SQL. |
| 138 | `CREATE TABLE public.attachments (` | Cria uma tabela no banco de dados Supabase/PostgreSQL. |
| 139 | `  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),` | Define identificador unico principal do registro. |
| 140 | `  task_id UUID NOT NULL REFERENCES public.tasks(id) ON DELETE CASCADE,` | Define relacionamento entre tabelas por chave estrangeira. |
| 141 | `  file_name TEXT NOT NULL,` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 142 | `  storage_path TEXT NOT NULL,` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 143 | `  mime_type TEXT,` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 144 | `  size_bytes BIGINT,` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 145 | `  uploaded_by UUID REFERENCES auth.users(id) ON DELETE SET NULL,` | Define relacionamento entre tabelas por chave estrangeira. |
| 146 | `  created_at TIMESTAMPTZ NOT NULL DEFAULT now()` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 147 | `);` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 148 | `GRANT SELECT, INSERT, UPDATE, DELETE ON public.attachments TO authenticated;` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 149 | `GRANT ALL ON public.attachments TO service_role;` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 150 | `ALTER TABLE public.attachments ENABLE ROW LEVEL SECURITY;` | Altera uma tabela existente, normalmente adicionando campos, chaves ou politicas. |
| 151 | `CREATE POLICY "att_all_auth" ON public.attachments FOR ALL TO authenticated USING (true) WITH CHECK (true);` | Cria uma regra de seguranca RLS para controlar acesso aos dados. |
| 152 | `(linha em branco)` | Separa blocos de codigo para melhorar a leitura. |
| 153 | `-- ============ TASK HISTORY ============` | Comentario da migration; explica a intencao daquele trecho SQL. |
| 154 | `CREATE TABLE public.task_history (` | Cria uma tabela no banco de dados Supabase/PostgreSQL. |
| 155 | `  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),` | Define identificador unico principal do registro. |
| 156 | `  task_id UUID NOT NULL REFERENCES public.tasks(id) ON DELETE CASCADE,` | Define relacionamento entre tabelas por chave estrangeira. |
| 157 | `  user_id UUID REFERENCES auth.users(id) ON DELETE SET NULL,` | Define relacionamento entre tabelas por chave estrangeira. |
| 158 | `  action TEXT NOT NULL,` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 159 | `  details JSONB,` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 160 | `  created_at TIMESTAMPTZ NOT NULL DEFAULT now()` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 161 | `);` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 162 | `GRANT SELECT, INSERT, UPDATE, DELETE ON public.task_history TO authenticated;` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 163 | `GRANT ALL ON public.task_history TO service_role;` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 164 | `ALTER TABLE public.task_history ENABLE ROW LEVEL SECURITY;` | Altera uma tabela existente, normalmente adicionando campos, chaves ou politicas. |
| 165 | `CREATE POLICY "hist_select_all" ON public.task_history FOR SELECT TO authenticated USING (true);` | Cria uma regra de seguranca RLS para controlar acesso aos dados. |
| 166 | `CREATE POLICY "hist_insert_self" ON public.task_history FOR INSERT TO authenticated WITH CHECK (auth.uid() = user_id);` | Cria uma regra de seguranca RLS para controlar acesso aos dados. |
| 167 | `(linha em branco)` | Separa blocos de codigo para melhorar a leitura. |
| 168 | `-- ============ UPDATED_AT TRIGGER ============` | Comentario da migration; explica a intencao daquele trecho SQL. |
| 169 | `CREATE OR REPLACE FUNCTION public.set_updated_at()` | Define uma funcao no banco para reutilizar logica SQL. |
| 170 | `RETURNS TRIGGER LANGUAGE plpgsql SET search_path = public AS $$` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 171 | `BEGIN NEW.updated_at = now(); RETURN NEW; END; $$;` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 172 | `(linha em branco)` | Separa blocos de codigo para melhorar a leitura. |
| 173 | `CREATE TRIGGER trg_profiles_u BEFORE UPDATE ON public.profiles FOR EACH ROW EXECUTE FUNCTION public.set_updated_at();` | Cria gatilho que executa uma funcao automaticamente em eventos da tabela. |
| 174 | `CREATE TRIGGER trg_clients_u BEFORE UPDATE ON public.clients FOR EACH ROW EXECUTE FUNCTION public.set_updated_at();` | Cria gatilho que executa uma funcao automaticamente em eventos da tabela. |
| 175 | `CREATE TRIGGER trg_cols_u BEFORE UPDATE ON public.kanban_columns FOR EACH ROW EXECUTE FUNCTION public.set_updated_at();` | Cria gatilho que executa uma funcao automaticamente em eventos da tabela. |
| 176 | `CREATE TRIGGER trg_tasks_u BEFORE UPDATE ON public.tasks FOR EACH ROW EXECUTE FUNCTION public.set_updated_at();` | Cria gatilho que executa uma funcao automaticamente em eventos da tabela. |
| 177 | `(linha em branco)` | Separa blocos de codigo para melhorar a leitura. |
| 178 | `-- ============ SIGNUP HANDLER ============` | Comentario da migration; explica a intencao daquele trecho SQL. |
| 179 | `CREATE OR REPLACE FUNCTION public.handle_new_user()` | Define uma funcao no banco para reutilizar logica SQL. |
| 180 | `RETURNS TRIGGER LANGUAGE plpgsql SECURITY DEFINER SET search_path = public AS $$` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 181 | `DECLARE` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 182 | `  user_count INT;` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 183 | `BEGIN` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 184 | `  INSERT INTO public.profiles (id, full_name, email)` | Insere dados iniciais ou registros de apoio. |
| 185 | `  VALUES (NEW.id, COALESCE(NEW.raw_user_meta_data->>'full_name', NEW.raw_user_meta_data->>'name', split_part(NEW.email, '@', 1)), NEW.email);` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 186 | `(linha em branco)` | Separa blocos de codigo para melhorar a leitura. |
| 187 | `  SELECT COUNT(*) INTO user_count FROM public.profiles;` | Consulta dados ou valida uma condicao no banco. |
| 188 | `  IF user_count = 1 THEN` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 189 | `    INSERT INTO public.user_roles (user_id, role) VALUES (NEW.id, 'admin');` | Insere dados iniciais ou registros de apoio. |
| 190 | `  ELSE` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 191 | `    INSERT INTO public.user_roles (user_id, role) VALUES (NEW.id, 'member');` | Insere dados iniciais ou registros de apoio. |
| 192 | `  END IF;` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 193 | `  RETURN NEW;` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 194 | `END; $$;` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 195 | `(linha em branco)` | Separa blocos de codigo para melhorar a leitura. |
| 196 | `CREATE TRIGGER on_auth_user_created AFTER INSERT ON auth.users` | Cria gatilho que executa uma funcao automaticamente em eventos da tabela. |
| 197 | `  FOR EACH ROW EXECUTE FUNCTION public.handle_new_user();` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 198 | `(linha em branco)` | Separa blocos de codigo para melhorar a leitura. |
| 199 | `-- ============ DEFAULT KANBAN COLUMNS ============` | Comentario da migration; explica a intencao daquele trecho SQL. |
| 200 | `INSERT INTO public.kanban_columns (name, color, position) VALUES` | Insere dados iniciais ou registros de apoio. |
| 201 | `  ('A Fazer', '#1e3a8a', 0),` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 202 | `  ('Em Andamento', '#2563eb', 1),` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 203 | `  ('Em Revisão', '#7c3aed', 2),` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 204 | `  ('Concluído', '#059669', 3);` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 205 | `(linha em branco)` | Separa blocos de codigo para melhorar a leitura. |
