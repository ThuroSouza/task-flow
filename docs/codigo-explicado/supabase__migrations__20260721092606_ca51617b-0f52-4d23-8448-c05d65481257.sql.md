# supabase/migrations/20260721092606_ca51617b-0f52-4d23-8448-c05d65481257.sql

Tipo: Migration SQL do Supabase.

Este documento foi gerado para estudo e manutencao. Ele preserva o codigo original e explica linha por linha sem alterar o comportamento do sistema publicado.

| Linha | Codigo original | Explicacao |
| ---: | --- | --- |
| 1 | `(linha em branco)` | Separa blocos de codigo para melhorar a leitura. |
| 2 | `DROP TRIGGER IF EXISTS trg_notify_task_assignment ON public.tasks;` | Remove objeto antigo para permitir recriacao ou limpeza controlada. |
| 3 | `CREATE TRIGGER trg_notify_task_assignment` | Cria gatilho que executa uma funcao automaticamente em eventos da tabela. |
| 4 | `AFTER INSERT OR UPDATE OF assignee_id ON public.tasks` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 5 | `FOR EACH ROW EXECUTE FUNCTION public.notify_task_assignment();` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 6 | `(linha em branco)` | Separa blocos de codigo para melhorar a leitura. |
| 7 | `DROP TRIGGER IF EXISTS trg_notify_subtask_assignment ON public.subtasks;` | Remove objeto antigo para permitir recriacao ou limpeza controlada. |
| 8 | `CREATE TRIGGER trg_notify_subtask_assignment` | Cria gatilho que executa uma funcao automaticamente em eventos da tabela. |
| 9 | `AFTER INSERT OR UPDATE OF assignee_id ON public.subtasks` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 10 | `FOR EACH ROW EXECUTE FUNCTION public.notify_subtask_assignment();` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 11 | `(linha em branco)` | Separa blocos de codigo para melhorar a leitura. |
| 12 | `DROP TRIGGER IF EXISTS trg_notify_task_comment ON public.comments;` | Remove objeto antigo para permitir recriacao ou limpeza controlada. |
| 13 | `CREATE TRIGGER trg_notify_task_comment` | Cria gatilho que executa uma funcao automaticamente em eventos da tabela. |
| 14 | `AFTER INSERT ON public.comments` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 15 | `FOR EACH ROW EXECUTE FUNCTION public.notify_task_comment();` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 16 | `(linha em branco)` | Separa blocos de codigo para melhorar a leitura. |
| 17 | `DROP TRIGGER IF EXISTS trg_notify_task_attachment ON public.attachments;` | Remove objeto antigo para permitir recriacao ou limpeza controlada. |
| 18 | `CREATE TRIGGER trg_notify_task_attachment` | Cria gatilho que executa uma funcao automaticamente em eventos da tabela. |
| 19 | `AFTER INSERT ON public.attachments` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 20 | `FOR EACH ROW EXECUTE FUNCTION public.notify_task_attachment();` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 21 | `(linha em branco)` | Separa blocos de codigo para melhorar a leitura. |
