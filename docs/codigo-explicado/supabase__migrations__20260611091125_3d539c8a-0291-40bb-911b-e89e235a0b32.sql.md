# supabase/migrations/20260611091125_3d539c8a-0291-40bb-911b-e89e235a0b32.sql

Tipo: Migration SQL do Supabase.

Este documento foi gerado para estudo e manutencao. Ele preserva o codigo original e explica linha por linha sem alterar o comportamento do sistema publicado.

| Linha | Codigo original | Explicacao |
| ---: | --- | --- |
| 1 | `(linha em branco)` | Separa blocos de codigo para melhorar a leitura. |
| 2 | `-- Custom task statuses with color and "active" highlight flag` | Comentario da migration; explica a intencao daquele trecho SQL. |
| 3 | `CREATE TABLE public.task_statuses (` | Cria uma tabela no banco de dados Supabase/PostgreSQL. |
| 4 | `  id uuid PRIMARY KEY DEFAULT gen_random_uuid(),` | Define identificador unico principal do registro. |
| 5 | `  name text NOT NULL,` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 6 | `  color text NOT NULL DEFAULT '#64748b',` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 7 | `  position integer NOT NULL DEFAULT 0,` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 8 | `  is_completed boolean NOT NULL DEFAULT false,` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 9 | `  is_active boolean NOT NULL DEFAULT false,` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 10 | `  created_by uuid REFERENCES auth.users(id) ON DELETE SET NULL,` | Define relacionamento entre tabelas por chave estrangeira. |
| 11 | `  created_at timestamptz NOT NULL DEFAULT now(),` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 12 | `  updated_at timestamptz NOT NULL DEFAULT now()` | Atualiza registros existentes no banco. |
| 13 | `);` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 14 | `(linha em branco)` | Separa blocos de codigo para melhorar a leitura. |
| 15 | `GRANT SELECT, INSERT, UPDATE, DELETE ON public.task_statuses TO authenticated;` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 16 | `GRANT ALL ON public.task_statuses TO service_role;` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 17 | `(linha em branco)` | Separa blocos de codigo para melhorar a leitura. |
| 18 | `ALTER TABLE public.task_statuses ENABLE ROW LEVEL SECURITY;` | Altera uma tabela existente, normalmente adicionando campos, chaves ou politicas. |
| 19 | `(linha em branco)` | Separa blocos de codigo para melhorar a leitura. |
| 20 | `CREATE POLICY "task_statuses_all_auth" ON public.task_statuses` | Cria uma regra de seguranca RLS para controlar acesso aos dados. |
| 21 | `  FOR ALL TO authenticated USING (true) WITH CHECK (true);` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 22 | `(linha em branco)` | Separa blocos de codigo para melhorar a leitura. |
| 23 | `CREATE TRIGGER trg_task_statuses_u BEFORE UPDATE ON public.task_statuses` | Cria gatilho que executa uma funcao automaticamente em eventos da tabela. |
| 24 | `  FOR EACH ROW EXECUTE FUNCTION public.set_updated_at();` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 25 | `(linha em branco)` | Separa blocos de codigo para melhorar a leitura. |
| 26 | `-- Add status_id to tasks (custom status reference)` | Comentario da migration; explica a intencao daquele trecho SQL. |
| 27 | `ALTER TABLE public.tasks ADD COLUMN status_id uuid REFERENCES public.task_statuses(id) ON DELETE SET NULL;` | Altera uma tabela existente, normalmente adicionando campos, chaves ou politicas. |
| 28 | `CREATE INDEX tasks_status_id_idx ON public.tasks(status_id);` | Cria indice para acelerar consultas frequentes. |
| 29 | `(linha em branco)` | Separa blocos de codigo para melhorar a leitura. |
| 30 | `-- Seed defaults` | Comentario da migration; explica a intencao daquele trecho SQL. |
| 31 | `INSERT INTO public.task_statuses (name, color, position, is_completed, is_active) VALUES` | Insere dados iniciais ou registros de apoio. |
| 32 | `  ('A Fazer', '#64748b', 0, false, false),` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 33 | `  ('Em Andamento', '#3b82f6', 1, false, true),` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 34 | `  ('Em Revisão', '#f59e0b', 2, false, false),` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 35 | `  ('Concluída', '#22c55e', 3, true, false);` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 36 | `(linha em branco)` | Separa blocos de codigo para melhorar a leitura. |
| 37 | `-- Backfill status_id based on existing enum status` | Comentario da migration; explica a intencao daquele trecho SQL. |
| 38 | `UPDATE public.tasks t SET status_id = s.id` | Atualiza registros existentes no banco. |
| 39 | `FROM public.task_statuses s` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 40 | `WHERE (t.status::text = 'todo' AND s.name = 'A Fazer')` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 41 | `   OR (t.status::text = 'in_progress' AND s.name = 'Em Andamento')` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 42 | `   OR (t.status::text = 'review' AND s.name = 'Em Revisão')` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 43 | `   OR (t.status::text = 'done' AND s.name = 'Concluída');` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 44 | `(linha em branco)` | Separa blocos de codigo para melhorar a leitura. |
