# supabase/migrations/20260720203828_4a127ff1-1339-464a-a60f-f68e0782fbec.sql

Tipo: Migration SQL do Supabase.

Este documento foi gerado para estudo e manutencao. Ele preserva o codigo original e explica linha por linha sem alterar o comportamento do sistema publicado.

| Linha | Codigo original | Explicacao |
| ---: | --- | --- |
| 1 | `(linha em branco)` | Separa blocos de codigo para melhorar a leitura. |
| 2 | `-- Attach missing notification/sync triggers so they actually fire.` | Comentario da migration; explica a intencao daquele trecho SQL. |
| 3 | `(linha em branco)` | Separa blocos de codigo para melhorar a leitura. |
| 4 | `DROP TRIGGER IF EXISTS trg_notify_task_assignment ON public.tasks;` | Remove objeto antigo para permitir recriacao ou limpeza controlada. |
| 5 | `CREATE TRIGGER trg_notify_task_assignment` | Cria gatilho que executa uma funcao automaticamente em eventos da tabela. |
| 6 | `AFTER INSERT OR UPDATE OF assignee_id ON public.tasks` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 7 | `FOR EACH ROW EXECUTE FUNCTION public.notify_task_assignment();` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 8 | `(linha em branco)` | Separa blocos de codigo para melhorar a leitura. |
| 9 | `DROP TRIGGER IF EXISTS trg_notify_subtask_assignment ON public.subtasks;` | Remove objeto antigo para permitir recriacao ou limpeza controlada. |
| 10 | `CREATE TRIGGER trg_notify_subtask_assignment` | Cria gatilho que executa uma funcao automaticamente em eventos da tabela. |
| 11 | `AFTER INSERT OR UPDATE OF assignee_id ON public.subtasks` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 12 | `FOR EACH ROW EXECUTE FUNCTION public.notify_subtask_assignment();` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 13 | `(linha em branco)` | Separa blocos de codigo para melhorar a leitura. |
| 14 | `DROP TRIGGER IF EXISTS trg_notify_task_comment ON public.comments;` | Remove objeto antigo para permitir recriacao ou limpeza controlada. |
| 15 | `CREATE TRIGGER trg_notify_task_comment` | Cria gatilho que executa uma funcao automaticamente em eventos da tabela. |
| 16 | `AFTER INSERT ON public.comments` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 17 | `FOR EACH ROW EXECUTE FUNCTION public.notify_task_comment();` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 18 | `(linha em branco)` | Separa blocos de codigo para melhorar a leitura. |
| 19 | `DROP TRIGGER IF EXISTS trg_notify_task_attachment ON public.attachments;` | Remove objeto antigo para permitir recriacao ou limpeza controlada. |
| 20 | `CREATE TRIGGER trg_notify_task_attachment` | Cria gatilho que executa uma funcao automaticamente em eventos da tabela. |
| 21 | `AFTER INSERT ON public.attachments` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 22 | `FOR EACH ROW EXECUTE FUNCTION public.notify_task_attachment();` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 23 | `(linha em branco)` | Separa blocos de codigo para melhorar a leitura. |
| 24 | `DROP TRIGGER IF EXISTS trg_sync_subtask_completed_at ON public.subtasks;` | Remove objeto antigo para permitir recriacao ou limpeza controlada. |
| 25 | `CREATE TRIGGER trg_sync_subtask_completed_at` | Cria gatilho que executa uma funcao automaticamente em eventos da tabela. |
| 26 | `BEFORE INSERT OR UPDATE OF done ON public.subtasks` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 27 | `FOR EACH ROW EXECUTE FUNCTION public.sync_subtask_completed_at();` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 28 | `(linha em branco)` | Separa blocos de codigo para melhorar a leitura. |
| 29 | `DROP TRIGGER IF EXISTS trg_set_task_created_by ON public.tasks;` | Remove objeto antigo para permitir recriacao ou limpeza controlada. |
| 30 | `CREATE TRIGGER trg_set_task_created_by` | Cria gatilho que executa uma funcao automaticamente em eventos da tabela. |
| 31 | `BEFORE INSERT ON public.tasks` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 32 | `FOR EACH ROW EXECUTE FUNCTION public.set_task_created_by();` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 33 | `(linha em branco)` | Separa blocos de codigo para melhorar a leitura. |
