# supabase/migrations/20260617140454_bb9df6d2-931f-415f-b758-3f4651fcc09d.sql

Tipo: Migration SQL do Supabase.

Este documento foi gerado para estudo e manutencao. Ele preserva o codigo original e explica linha por linha sem alterar o comportamento do sistema publicado.

| Linha | Codigo original | Explicacao |
| ---: | --- | --- |
| 1 | `DROP POLICY IF EXISTS kanban_columns_select ON public.kanban_columns;` | Remove objeto antigo para permitir recriacao ou limpeza controlada. |
| 2 | `CREATE POLICY kanban_columns_select ON public.kanban_columns` | Cria uma regra de seguranca RLS para controlar acesso aos dados. |
| 3 | `  FOR SELECT USING (public.has_role(auth.uid(), 'admin'::app_role) OR created_by = auth.uid());` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 4 | `(linha em branco)` | Separa blocos de codigo para melhorar a leitura. |
| 5 | `DROP POLICY IF EXISTS task_statuses_select ON public.task_statuses;` | Remove objeto antigo para permitir recriacao ou limpeza controlada. |
| 6 | `CREATE POLICY task_statuses_select ON public.task_statuses` | Cria uma regra de seguranca RLS para controlar acesso aos dados. |
| 7 | `  FOR SELECT USING (public.has_role(auth.uid(), 'admin'::app_role) OR created_by = auth.uid());` | Executa parte da mudanca estrutural ou de seguranca do banco. |
