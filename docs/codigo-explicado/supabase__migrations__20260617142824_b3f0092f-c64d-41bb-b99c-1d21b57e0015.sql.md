# supabase/migrations/20260617142824_b3f0092f-c64d-41bb-b99c-1d21b57e0015.sql

Tipo: Migration SQL do Supabase.

Este documento foi gerado para estudo e manutencao. Ele preserva o codigo original e explica linha por linha sem alterar o comportamento do sistema publicado.

| Linha | Codigo original | Explicacao |
| ---: | --- | --- |
| 1 | `(linha em branco)` | Separa blocos de codigo para melhorar a leitura. |
| 2 | `-- ============================================================` | Comentario da migration; explica a intencao daquele trecho SQL. |
| 3 | `-- Make tags/statuses/columns GLOBAL (admin-managed)` | Comentario da migration; explica a intencao daquele trecho SQL. |
| 4 | `-- Per-user ordering preferences for columns and tasks` | Comentario da migration; explica a intencao daquele trecho SQL. |
| 5 | `-- ============================================================` | Comentario da migration; explica a intencao daquele trecho SQL. |
| 6 | `(linha em branco)` | Separa blocos de codigo para melhorar a leitura. |
| 7 | `-- ---------- 1) Dedup task_tags by normalized name ----------` | Comentario da migration; explica a intencao daquele trecho SQL. |
| 8 | `WITH ranked AS (` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 9 | `  SELECT id,` | Consulta dados ou valida uma condicao no banco. |
| 10 | `    ROW_NUMBER() OVER (PARTITION BY lower(btrim(name)) ORDER BY created_at NULLS LAST, id) AS rn,` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 11 | `    FIRST_VALUE(id) OVER (PARTITION BY lower(btrim(name)) ORDER BY created_at NULLS LAST, id) AS keeper_id` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 12 | `  FROM public.task_tags` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 13 | `)` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 14 | `UPDATE public.task_tag_links l SET tag_id = r.keeper_id` | Atualiza registros existentes no banco. |
| 15 | `FROM ranked r WHERE l.tag_id = r.id AND r.rn > 1;` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 16 | `(linha em branco)` | Separa blocos de codigo para melhorar a leitura. |
| 17 | `WITH ranked AS (` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 18 | `  SELECT id,` | Consulta dados ou valida uma condicao no banco. |
| 19 | `    ROW_NUMBER() OVER (PARTITION BY lower(btrim(name)) ORDER BY created_at NULLS LAST, id) AS rn,` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 20 | `    FIRST_VALUE(id) OVER (PARTITION BY lower(btrim(name)) ORDER BY created_at NULLS LAST, id) AS keeper_id` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 21 | `  FROM public.task_tags` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 22 | `)` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 23 | `UPDATE public.tasks t SET tag_id = r.keeper_id` | Atualiza registros existentes no banco. |
| 24 | `FROM ranked r WHERE t.tag_id = r.id AND r.rn > 1;` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 25 | `(linha em branco)` | Separa blocos de codigo para melhorar a leitura. |
| 26 | `-- Collapse duplicate (task_id, tag_id) link rows that may have appeared` | Comentario da migration; explica a intencao daquele trecho SQL. |
| 27 | `DELETE FROM public.task_tag_links a USING public.task_tag_links b` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 28 | `WHERE a.ctid < b.ctid AND a.task_id = b.task_id AND a.tag_id = b.tag_id;` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 29 | `(linha em branco)` | Separa blocos de codigo para melhorar a leitura. |
| 30 | `DELETE FROM public.task_tags WHERE id IN (` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 31 | `  SELECT id FROM (` | Consulta dados ou valida uma condicao no banco. |
| 32 | `    SELECT id, ROW_NUMBER() OVER (PARTITION BY lower(btrim(name)) ORDER BY created_at NULLS LAST, id) AS rn` | Consulta dados ou valida uma condicao no banco. |
| 33 | `    FROM public.task_tags` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 34 | `  ) s WHERE rn > 1` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 35 | `);` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 36 | `(linha em branco)` | Separa blocos de codigo para melhorar a leitura. |
| 37 | `-- ---------- 2) Dedup task_statuses by normalized name ----------` | Comentario da migration; explica a intencao daquele trecho SQL. |
| 38 | `WITH ranked AS (` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 39 | `  SELECT id,` | Consulta dados ou valida uma condicao no banco. |
| 40 | `    ROW_NUMBER() OVER (PARTITION BY lower(btrim(name)) ORDER BY created_at NULLS LAST, id) AS rn,` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 41 | `    FIRST_VALUE(id) OVER (PARTITION BY lower(btrim(name)) ORDER BY created_at NULLS LAST, id) AS keeper_id` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 42 | `  FROM public.task_statuses` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 43 | `)` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 44 | `UPDATE public.tasks t SET status_id = r.keeper_id` | Atualiza registros existentes no banco. |
| 45 | `FROM ranked r WHERE t.status_id = r.id AND r.rn > 1;` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 46 | `(linha em branco)` | Separa blocos de codigo para melhorar a leitura. |
| 47 | `DELETE FROM public.task_statuses WHERE id IN (` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 48 | `  SELECT id FROM (` | Consulta dados ou valida uma condicao no banco. |
| 49 | `    SELECT id, ROW_NUMBER() OVER (PARTITION BY lower(btrim(name)) ORDER BY created_at NULLS LAST, id) AS rn` | Consulta dados ou valida uma condicao no banco. |
| 50 | `    FROM public.task_statuses` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 51 | `  ) s WHERE rn > 1` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 52 | `);` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 53 | `(linha em branco)` | Separa blocos de codigo para melhorar a leitura. |
| 54 | `-- Reset position to be sequential (0,1,2…) using current order` | Comentario da migration; explica a intencao daquele trecho SQL. |
| 55 | `WITH ord AS (` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 56 | `  SELECT id, ROW_NUMBER() OVER (ORDER BY position, created_at NULLS LAST, id) - 1 AS new_pos` | Consulta dados ou valida uma condicao no banco. |
| 57 | `  FROM public.task_statuses` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 58 | `)` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 59 | `UPDATE public.task_statuses s SET position = o.new_pos FROM ord o WHERE s.id = o.id;` | Atualiza registros existentes no banco. |
| 60 | `(linha em branco)` | Separa blocos de codigo para melhorar a leitura. |
| 61 | `-- ---------- 3) Dedup kanban_columns by normalized name ----------` | Comentario da migration; explica a intencao daquele trecho SQL. |
| 62 | `WITH ranked AS (` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 63 | `  SELECT id,` | Consulta dados ou valida uma condicao no banco. |
| 64 | `    ROW_NUMBER() OVER (PARTITION BY lower(btrim(name)) ORDER BY created_at NULLS LAST, id) AS rn,` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 65 | `    FIRST_VALUE(id) OVER (PARTITION BY lower(btrim(name)) ORDER BY created_at NULLS LAST, id) AS keeper_id` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 66 | `  FROM public.kanban_columns` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 67 | `)` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 68 | `UPDATE public.tasks t SET column_id = r.keeper_id` | Atualiza registros existentes no banco. |
| 69 | `FROM ranked r WHERE t.column_id = r.id AND r.rn > 1;` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 70 | `(linha em branco)` | Separa blocos de codigo para melhorar a leitura. |
| 71 | `DELETE FROM public.kanban_columns WHERE id IN (` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 72 | `  SELECT id FROM (` | Consulta dados ou valida uma condicao no banco. |
| 73 | `    SELECT id, ROW_NUMBER() OVER (PARTITION BY lower(btrim(name)) ORDER BY created_at NULLS LAST, id) AS rn` | Consulta dados ou valida uma condicao no banco. |
| 74 | `    FROM public.kanban_columns` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 75 | `  ) s WHERE rn > 1` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 76 | `);` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 77 | `(linha em branco)` | Separa blocos de codigo para melhorar a leitura. |
| 78 | `WITH ord AS (` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 79 | `  SELECT id, ROW_NUMBER() OVER (ORDER BY position, created_at NULLS LAST, id) - 1 AS new_pos` | Consulta dados ou valida uma condicao no banco. |
| 80 | `  FROM public.kanban_columns` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 81 | `)` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 82 | `UPDATE public.kanban_columns k SET position = o.new_pos FROM ord o WHERE k.id = o.id;` | Atualiza registros existentes no banco. |
| 83 | `(linha em branco)` | Separa blocos de codigo para melhorar a leitura. |
| 84 | `-- ---------- 4) Replace RLS: SELECT for all authenticated, write for admins only ----------` | Comentario da migration; explica a intencao daquele trecho SQL. |
| 85 | `(linha em branco)` | Separa blocos de codigo para melhorar a leitura. |
| 86 | `-- task_tags` | Comentario da migration; explica a intencao daquele trecho SQL. |
| 87 | `DROP POLICY IF EXISTS task_tags_select ON public.task_tags;` | Remove objeto antigo para permitir recriacao ou limpeza controlada. |
| 88 | `DROP POLICY IF EXISTS task_tags_insert ON public.task_tags;` | Remove objeto antigo para permitir recriacao ou limpeza controlada. |
| 89 | `DROP POLICY IF EXISTS task_tags_update ON public.task_tags;` | Remove objeto antigo para permitir recriacao ou limpeza controlada. |
| 90 | `DROP POLICY IF EXISTS task_tags_delete ON public.task_tags;` | Remove objeto antigo para permitir recriacao ou limpeza controlada. |
| 91 | `DROP POLICY IF EXISTS task_tags_modify ON public.task_tags;` | Remove objeto antigo para permitir recriacao ou limpeza controlada. |
| 92 | `DROP POLICY IF EXISTS task_tags_all ON public.task_tags;` | Remove objeto antigo para permitir recriacao ou limpeza controlada. |
| 93 | `CREATE POLICY task_tags_select_all ON public.task_tags FOR SELECT TO authenticated USING (true);` | Cria uma regra de seguranca RLS para controlar acesso aos dados. |
| 94 | `CREATE POLICY task_tags_admin_write ON public.task_tags FOR ALL TO authenticated` | Cria uma regra de seguranca RLS para controlar acesso aos dados. |
| 95 | `  USING (public.has_role(auth.uid(), 'admin'))` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 96 | `  WITH CHECK (public.has_role(auth.uid(), 'admin'));` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 97 | `(linha em branco)` | Separa blocos de codigo para melhorar a leitura. |
| 98 | `-- task_statuses` | Comentario da migration; explica a intencao daquele trecho SQL. |
| 99 | `DROP POLICY IF EXISTS task_statuses_select ON public.task_statuses;` | Remove objeto antigo para permitir recriacao ou limpeza controlada. |
| 100 | `DROP POLICY IF EXISTS task_statuses_insert ON public.task_statuses;` | Remove objeto antigo para permitir recriacao ou limpeza controlada. |
| 101 | `DROP POLICY IF EXISTS task_statuses_update ON public.task_statuses;` | Remove objeto antigo para permitir recriacao ou limpeza controlada. |
| 102 | `DROP POLICY IF EXISTS task_statuses_delete ON public.task_statuses;` | Remove objeto antigo para permitir recriacao ou limpeza controlada. |
| 103 | `DROP POLICY IF EXISTS task_statuses_all ON public.task_statuses;` | Remove objeto antigo para permitir recriacao ou limpeza controlada. |
| 104 | `CREATE POLICY task_statuses_select_all ON public.task_statuses FOR SELECT TO authenticated USING (true);` | Cria uma regra de seguranca RLS para controlar acesso aos dados. |
| 105 | `CREATE POLICY task_statuses_admin_write ON public.task_statuses FOR ALL TO authenticated` | Cria uma regra de seguranca RLS para controlar acesso aos dados. |
| 106 | `  USING (public.has_role(auth.uid(), 'admin'))` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 107 | `  WITH CHECK (public.has_role(auth.uid(), 'admin'));` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 108 | `(linha em branco)` | Separa blocos de codigo para melhorar a leitura. |
| 109 | `-- kanban_columns` | Comentario da migration; explica a intencao daquele trecho SQL. |
| 110 | `DROP POLICY IF EXISTS kanban_columns_select ON public.kanban_columns;` | Remove objeto antigo para permitir recriacao ou limpeza controlada. |
| 111 | `DROP POLICY IF EXISTS kanban_columns_insert ON public.kanban_columns;` | Remove objeto antigo para permitir recriacao ou limpeza controlada. |
| 112 | `DROP POLICY IF EXISTS kanban_columns_update ON public.kanban_columns;` | Remove objeto antigo para permitir recriacao ou limpeza controlada. |
| 113 | `DROP POLICY IF EXISTS kanban_columns_delete ON public.kanban_columns;` | Remove objeto antigo para permitir recriacao ou limpeza controlada. |
| 114 | `DROP POLICY IF EXISTS kanban_columns_all ON public.kanban_columns;` | Remove objeto antigo para permitir recriacao ou limpeza controlada. |
| 115 | `CREATE POLICY kanban_columns_select_all ON public.kanban_columns FOR SELECT TO authenticated USING (true);` | Cria uma regra de seguranca RLS para controlar acesso aos dados. |
| 116 | `CREATE POLICY kanban_columns_admin_write ON public.kanban_columns FOR ALL TO authenticated` | Cria uma regra de seguranca RLS para controlar acesso aos dados. |
| 117 | `  USING (public.has_role(auth.uid(), 'admin'))` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 118 | `  WITH CHECK (public.has_role(auth.uid(), 'admin'));` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 119 | `(linha em branco)` | Separa blocos de codigo para melhorar a leitura. |
| 120 | `-- ---------- 5) Stop auto-seeding columns/statuses for new users ----------` | Comentario da migration; explica a intencao daquele trecho SQL. |
| 121 | `CREATE OR REPLACE FUNCTION public.handle_new_user()` | Define uma funcao no banco para reutilizar logica SQL. |
| 122 | `RETURNS trigger LANGUAGE plpgsql SECURITY DEFINER SET search_path = public AS $$` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 123 | `DECLARE user_count INT;` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 124 | `BEGIN` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 125 | `  INSERT INTO public.profiles (id, full_name, email)` | Insere dados iniciais ou registros de apoio. |
| 126 | `  VALUES (NEW.id, COALESCE(NEW.raw_user_meta_data->>'full_name', NEW.raw_user_meta_data->>'name', split_part(NEW.email, '@', 1)), NEW.email);` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 127 | `(linha em branco)` | Separa blocos de codigo para melhorar a leitura. |
| 128 | `  SELECT COUNT(*) INTO user_count FROM public.profiles;` | Consulta dados ou valida uma condicao no banco. |
| 129 | `  IF user_count = 1 THEN` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 130 | `    INSERT INTO public.user_roles (user_id, role) VALUES (NEW.id, 'admin');` | Insere dados iniciais ou registros de apoio. |
| 131 | `  ELSE` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 132 | `    INSERT INTO public.user_roles (user_id, role) VALUES (NEW.id, 'member');` | Insere dados iniciais ou registros de apoio. |
| 133 | `  END IF;` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 134 | `(linha em branco)` | Separa blocos de codigo para melhorar a leitura. |
| 135 | `  RETURN NEW;` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 136 | `END $$;` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 137 | `(linha em branco)` | Separa blocos de codigo para melhorar a leitura. |
| 138 | `-- ---------- 6) Per-user column ordering ----------` | Comentario da migration; explica a intencao daquele trecho SQL. |
| 139 | `CREATE TABLE IF NOT EXISTS public.user_column_order (` | Cria uma tabela no banco de dados Supabase/PostgreSQL. |
| 140 | `  user_id uuid NOT NULL,` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 141 | `  column_id uuid NOT NULL REFERENCES public.kanban_columns(id) ON DELETE CASCADE,` | Define relacionamento entre tabelas por chave estrangeira. |
| 142 | `  position int NOT NULL DEFAULT 0,` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 143 | `  updated_at timestamptz NOT NULL DEFAULT now(),` | Atualiza registros existentes no banco. |
| 144 | `  PRIMARY KEY (user_id, column_id)` | Define identificador unico principal do registro. |
| 145 | `);` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 146 | `GRANT SELECT, INSERT, UPDATE, DELETE ON public.user_column_order TO authenticated;` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 147 | `GRANT ALL ON public.user_column_order TO service_role;` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 148 | `ALTER TABLE public.user_column_order ENABLE ROW LEVEL SECURITY;` | Altera uma tabela existente, normalmente adicionando campos, chaves ou politicas. |
| 149 | `DROP POLICY IF EXISTS uco_own ON public.user_column_order;` | Remove objeto antigo para permitir recriacao ou limpeza controlada. |
| 150 | `CREATE POLICY uco_own ON public.user_column_order FOR ALL TO authenticated` | Cria uma regra de seguranca RLS para controlar acesso aos dados. |
| 151 | `  USING (user_id = auth.uid()) WITH CHECK (user_id = auth.uid());` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 152 | `(linha em branco)` | Separa blocos de codigo para melhorar a leitura. |
| 153 | `-- ---------- 7) Per-user task ordering ----------` | Comentario da migration; explica a intencao daquele trecho SQL. |
| 154 | `CREATE TABLE IF NOT EXISTS public.user_task_order (` | Cria uma tabela no banco de dados Supabase/PostgreSQL. |
| 155 | `  user_id uuid NOT NULL,` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 156 | `  task_id uuid NOT NULL REFERENCES public.tasks(id) ON DELETE CASCADE,` | Define relacionamento entre tabelas por chave estrangeira. |
| 157 | `  position int NOT NULL DEFAULT 0,` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 158 | `  updated_at timestamptz NOT NULL DEFAULT now(),` | Atualiza registros existentes no banco. |
| 159 | `  PRIMARY KEY (user_id, task_id)` | Define identificador unico principal do registro. |
| 160 | `);` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 161 | `CREATE INDEX IF NOT EXISTS user_task_order_user_idx ON public.user_task_order(user_id);` | Cria indice para acelerar consultas frequentes. |
| 162 | `GRANT SELECT, INSERT, UPDATE, DELETE ON public.user_task_order TO authenticated;` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 163 | `GRANT ALL ON public.user_task_order TO service_role;` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 164 | `ALTER TABLE public.user_task_order ENABLE ROW LEVEL SECURITY;` | Altera uma tabela existente, normalmente adicionando campos, chaves ou politicas. |
| 165 | `DROP POLICY IF EXISTS uto_own ON public.user_task_order;` | Remove objeto antigo para permitir recriacao ou limpeza controlada. |
| 166 | `CREATE POLICY uto_own ON public.user_task_order FOR ALL TO authenticated` | Cria uma regra de seguranca RLS para controlar acesso aos dados. |
| 167 | `  USING (user_id = auth.uid()) WITH CHECK (user_id = auth.uid());` | Executa parte da mudanca estrutural ou de seguranca do banco. |
| 168 | `(linha em branco)` | Separa blocos de codigo para melhorar a leitura. |
